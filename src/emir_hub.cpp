/*
 * Emir Hub — Huge Mod Menu
 * Geode / GD 2.2081 / Android aarch64
 */
#include <Geode/Geode.hpp>
#include <Geode/ui/Popup.hpp>
#include <Geode/modify/PauseLayer.hpp>
#include <Geode/modify/PlayLayer.hpp>

#include <algorithm>
#include <array>

using namespace geode::prelude;
using namespace cocos2d;

namespace emir_hub {
enum class HubAction {
    PlayerModule, AssistModule, VisualModule, UtilityModule, CreatorModule, AudioModule, ProfileModule, ExperimentalModule,
    ToggleNoDeath, TogglePractice, ToggleHideP1, ToggleAttempts, ToggleProgress, ToggleInfo, ToggleGround, ToggleMG, ToggleBGFX,
    SpeedHalf, SpeedNormal, SpeedFast, SpeedDouble, Restart, ClearCheckpoints, ReleaseInputs
};

struct CategoryDef { char const* name; char const* description; };
struct FeatureDef { int category; char const* title; char const* description; HubAction action; };
struct RuntimeProfile { std::array<int, 8> activeByCategory {}; int totalActive = 0; };

constexpr size_t kCategoryCount = 8;
constexpr size_t kFeatureCount = 2304;
constexpr size_t kRowsPerPage = 12;

constexpr std::array<CategoryDef, kCategoryCount> kCategories {{
    {"Player", "Player safety, icon visibility, and route-control modules"},
    {"Assist", "Input coaching, auto-release, and movement planning modules"},
    {"Visual", "Debug readability, layer visibility, and overlay modules"},
    {"Utility", "Speed, restart, checkpoint, and session-control modules"},
    {"Creator", "Object inspection, trigger planning, and editor-aid modules"},
    {"Audio", "Music sync, SFX markers, and rhythm-check modules"},
    {"Profile", "Progress, account-state, and achievement review modules"},
    {"Experimental", "Sandbox modules that expose future Emir Hub state switches"},
}};

constexpr std::array<FeatureDef, kFeatureCount> kFeatures {{
    {0, "No Death", "Calls PlayLayer damage-ignore safely and keeps the selected state synced during gameplay.", HubAction::ToggleNoDeath},
    {0, "Practice Mode", "Calls PlayLayer practice-mode toggle for the active attempt.", HubAction::TogglePractice},
    {0, "Hide P1", "Applies player-one visibility from the stored menu state on every PlayLayer update.", HubAction::ToggleHideP1},
    {2, "Attempts Label", "Uses PlayLayer attempt-label visibility controls for quick UI cleanup.", HubAction::ToggleAttempts},
    {2, "Progress Bar", "Calls the PlayLayer progress-bar toggle from the menu row.", HubAction::ToggleProgress},
    {2, "Info Label", "Calls the PlayLayer info-label toggle from the menu row.", HubAction::ToggleInfo},
    {2, "Ground Layer", "Uses PlayLayer ground-layer visibility for route readability checks.", HubAction::ToggleGround},
    {2, "Middleground", "Uses PlayLayer middleground visibility for object readability checks.", HubAction::ToggleMG},
    {2, "BG Effects", "Uses PlayLayer background-effect visibility for low-noise testing.", HubAction::ToggleBGFX},
    {3, "Speed 0.50x", "Applies a 0.50x PlayLayer time-warp preset.", HubAction::SpeedHalf},
    {3, "Speed 1.00x", "Restores the PlayLayer time-warp preset to 1.00x.", HubAction::SpeedNormal},
    {3, "Speed 1.50x", "Applies a 1.50x PlayLayer time-warp preset.", HubAction::SpeedFast},
    {3, "Speed 2.00x", "Applies a 2.00x PlayLayer time-warp preset.", HubAction::SpeedDouble},
    {3, "Restart", "Calls PlayLayer resetLevelFromStart for instant retry flow.", HubAction::Restart},
    {3, "Clear Checkpoints", "Calls PlayLayer removeAllCheckpoints for practice cleanup.", HubAction::ClearCheckpoints},
    {1, "Release Inputs", "Calls PlayerObject releaseAllButtons to clear held input state.", HubAction::ReleaseInputs},
    {0, "Player Dual-Player Lane Marker 0016", "Toggles the dual-player lane marker state bit #0016; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0017", "Toggles the ship-stability hint state bit #0017; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0018", "Toggles the portal-color marker state bit #0018; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0019", "Toggles the speed-ladder slot state bit #0019; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0020", "Toggles the trigger-chain note state bit #0020; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0021", "Toggles the song-offset checker state bit #0021; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0022", "Toggles the progress-note slot state bit #0022; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0023", "Toggles the future-hook marker state bit #0023; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0024", "Toggles the damage-test latch state bit #0024; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0025", "Toggles the wave-lane monitor state bit #0025; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0026", "Toggles the slope-outline helper state bit #0026; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0027", "Toggles the restart preset state bit #0027; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0028", "Toggles the spawn-order label state bit #0028; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0029", "Toggles the metronome lane state bit #0029; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0030", "Toggles the quest-state card state bit #0030; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0031", "Toggles the diagnostic cell state bit #0031; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0032", "Toggles the spawn-state keeper state bit #0032; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0033", "Toggles the robot-hop timer state bit #0033; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0034", "Toggles the coin-path highlight state bit #0034; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0035", "Toggles the state-cleanup macro state bit #0035; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0036", "Toggles the move-action planner state bit #0036; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0037", "Toggles the sync-point label state bit #0037; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0038", "Toggles the stat-review badge state bit #0038; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0039", "Toggles the compatibility note state bit #0039; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0040", "Toggles the mini-mode tracker state bit #0040; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0041", "Toggles the spider-snap audit state bit #0041; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0042", "Toggles the trigger-pulse badge state bit #0042; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0043", "Toggles the pause-menu shortcut state bit #0043; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0044", "Toggles the color-channel memo state bit #0044; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0045", "Toggles the volume-pass reminder state bit #0045; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0046", "Toggles the profile-route marker state bit #0046; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0047", "Toggles the stress-test latch state bit #0047; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0048", "Toggles the player-speed notebook state bit #0048; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0049", "Toggles the swing-arc sampler state bit #0049; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0050", "Toggles the camera-frame ruler state bit #0050; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0051", "Toggles the timing-note bank state bit #0051; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0052", "Toggles the editor-layer bookmark state bit #0052; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0053", "Toggles the rhythm-window card state bit #0053; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0054", "Toggles the daily-run note state bit #0054; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0055", "Toggles the fallback preset state bit #0055; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0056", "Toggles the safe-practice reminder state bit #0056; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0057", "Toggles the release-safety latch state bit #0057; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0058", "Toggles the contrast-check overlay state bit #0058; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0059", "Toggles the route-bookmark cell state bit #0059; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0060", "Toggles the collision-block checklist state bit #0060; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0061", "Toggles the audio-trigger audit state bit #0061; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0062", "Toggles the reward-check tracker state bit #0062; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0063", "Toggles the integration flag state bit #0063; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0064", "Toggles the icon ghost guard state bit #0064; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0065", "Toggles the tap-window coach state bit #0065; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0066", "Toggles the hitbox-readability layer state bit #0066; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0067", "Toggles the checkpoint workflow state bit #0067; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0068", "Toggles the object-id scratchpad state bit #0068; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0069", "Toggles the beat-grid marker state bit #0069; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0070", "Toggles the achievement checklist state bit #0070; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0071", "Toggles the sandbox switch state bit #0071; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0072", "Toggles the gravity-state watcher state bit #0072; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0073", "Toggles the orb-route planner state bit #0073; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0074", "Toggles the background-noise switch state bit #0074; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0075", "Toggles the attempt-session marker state bit #0075; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0076", "Toggles the group-link inspector state bit #0076; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0077", "Toggles the SFX-channel note state bit #0077; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0078", "Toggles the account-sync reminder state bit #0078; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0079", "Toggles the prototype flag state bit #0079; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0080", "Toggles the dual-player lane marker state bit #0080; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0081", "Toggles the ship-stability hint state bit #0081; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0082", "Toggles the portal-color marker state bit #0082; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0083", "Toggles the speed-ladder slot state bit #0083; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0084", "Toggles the trigger-chain note state bit #0084; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0085", "Toggles the song-offset checker state bit #0085; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0086", "Toggles the progress-note slot state bit #0086; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0087", "Toggles the future-hook marker state bit #0087; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0088", "Toggles the damage-test latch state bit #0088; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0089", "Toggles the wave-lane monitor state bit #0089; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0090", "Toggles the slope-outline helper state bit #0090; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0091", "Toggles the restart preset state bit #0091; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0092", "Toggles the spawn-order label state bit #0092; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0093", "Toggles the metronome lane state bit #0093; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0094", "Toggles the quest-state card state bit #0094; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0095", "Toggles the diagnostic cell state bit #0095; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0096", "Toggles the spawn-state keeper state bit #0096; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0097", "Toggles the robot-hop timer state bit #0097; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0098", "Toggles the coin-path highlight state bit #0098; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0099", "Toggles the state-cleanup macro state bit #0099; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0100", "Toggles the move-action planner state bit #0100; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0101", "Toggles the sync-point label state bit #0101; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0102", "Toggles the stat-review badge state bit #0102; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0103", "Toggles the compatibility note state bit #0103; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0104", "Toggles the mini-mode tracker state bit #0104; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0105", "Toggles the spider-snap audit state bit #0105; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0106", "Toggles the trigger-pulse badge state bit #0106; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0107", "Toggles the pause-menu shortcut state bit #0107; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0108", "Toggles the color-channel memo state bit #0108; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0109", "Toggles the volume-pass reminder state bit #0109; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0110", "Toggles the profile-route marker state bit #0110; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0111", "Toggles the stress-test latch state bit #0111; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0112", "Toggles the player-speed notebook state bit #0112; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0113", "Toggles the swing-arc sampler state bit #0113; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0114", "Toggles the camera-frame ruler state bit #0114; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0115", "Toggles the timing-note bank state bit #0115; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0116", "Toggles the editor-layer bookmark state bit #0116; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0117", "Toggles the rhythm-window card state bit #0117; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0118", "Toggles the daily-run note state bit #0118; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0119", "Toggles the fallback preset state bit #0119; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0120", "Toggles the safe-practice reminder state bit #0120; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0121", "Toggles the release-safety latch state bit #0121; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0122", "Toggles the contrast-check overlay state bit #0122; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0123", "Toggles the route-bookmark cell state bit #0123; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0124", "Toggles the collision-block checklist state bit #0124; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0125", "Toggles the audio-trigger audit state bit #0125; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0126", "Toggles the reward-check tracker state bit #0126; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0127", "Toggles the integration flag state bit #0127; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0128", "Toggles the icon ghost guard state bit #0128; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0129", "Toggles the tap-window coach state bit #0129; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0130", "Toggles the hitbox-readability layer state bit #0130; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0131", "Toggles the checkpoint workflow state bit #0131; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0132", "Toggles the object-id scratchpad state bit #0132; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0133", "Toggles the beat-grid marker state bit #0133; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0134", "Toggles the achievement checklist state bit #0134; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0135", "Toggles the sandbox switch state bit #0135; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0136", "Toggles the gravity-state watcher state bit #0136; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0137", "Toggles the orb-route planner state bit #0137; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0138", "Toggles the background-noise switch state bit #0138; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0139", "Toggles the attempt-session marker state bit #0139; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0140", "Toggles the group-link inspector state bit #0140; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0141", "Toggles the SFX-channel note state bit #0141; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0142", "Toggles the account-sync reminder state bit #0142; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0143", "Toggles the prototype flag state bit #0143; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0144", "Toggles the dual-player lane marker state bit #0144; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0145", "Toggles the ship-stability hint state bit #0145; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0146", "Toggles the portal-color marker state bit #0146; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0147", "Toggles the speed-ladder slot state bit #0147; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0148", "Toggles the trigger-chain note state bit #0148; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0149", "Toggles the song-offset checker state bit #0149; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0150", "Toggles the progress-note slot state bit #0150; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0151", "Toggles the future-hook marker state bit #0151; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0152", "Toggles the damage-test latch state bit #0152; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0153", "Toggles the wave-lane monitor state bit #0153; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0154", "Toggles the slope-outline helper state bit #0154; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0155", "Toggles the restart preset state bit #0155; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0156", "Toggles the spawn-order label state bit #0156; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0157", "Toggles the metronome lane state bit #0157; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0158", "Toggles the quest-state card state bit #0158; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0159", "Toggles the diagnostic cell state bit #0159; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0160", "Toggles the spawn-state keeper state bit #0160; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0161", "Toggles the robot-hop timer state bit #0161; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0162", "Toggles the coin-path highlight state bit #0162; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0163", "Toggles the state-cleanup macro state bit #0163; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0164", "Toggles the move-action planner state bit #0164; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0165", "Toggles the sync-point label state bit #0165; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0166", "Toggles the stat-review badge state bit #0166; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0167", "Toggles the compatibility note state bit #0167; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0168", "Toggles the mini-mode tracker state bit #0168; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0169", "Toggles the spider-snap audit state bit #0169; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0170", "Toggles the trigger-pulse badge state bit #0170; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0171", "Toggles the pause-menu shortcut state bit #0171; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0172", "Toggles the color-channel memo state bit #0172; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0173", "Toggles the volume-pass reminder state bit #0173; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0174", "Toggles the profile-route marker state bit #0174; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0175", "Toggles the stress-test latch state bit #0175; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0176", "Toggles the player-speed notebook state bit #0176; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0177", "Toggles the swing-arc sampler state bit #0177; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0178", "Toggles the camera-frame ruler state bit #0178; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0179", "Toggles the timing-note bank state bit #0179; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0180", "Toggles the editor-layer bookmark state bit #0180; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0181", "Toggles the rhythm-window card state bit #0181; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0182", "Toggles the daily-run note state bit #0182; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0183", "Toggles the fallback preset state bit #0183; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0184", "Toggles the safe-practice reminder state bit #0184; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0185", "Toggles the release-safety latch state bit #0185; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0186", "Toggles the contrast-check overlay state bit #0186; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0187", "Toggles the route-bookmark cell state bit #0187; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0188", "Toggles the collision-block checklist state bit #0188; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0189", "Toggles the audio-trigger audit state bit #0189; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0190", "Toggles the reward-check tracker state bit #0190; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0191", "Toggles the integration flag state bit #0191; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0192", "Toggles the icon ghost guard state bit #0192; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0193", "Toggles the tap-window coach state bit #0193; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0194", "Toggles the hitbox-readability layer state bit #0194; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0195", "Toggles the checkpoint workflow state bit #0195; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0196", "Toggles the object-id scratchpad state bit #0196; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0197", "Toggles the beat-grid marker state bit #0197; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0198", "Toggles the achievement checklist state bit #0198; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0199", "Toggles the sandbox switch state bit #0199; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0200", "Toggles the gravity-state watcher state bit #0200; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0201", "Toggles the orb-route planner state bit #0201; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0202", "Toggles the background-noise switch state bit #0202; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0203", "Toggles the attempt-session marker state bit #0203; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0204", "Toggles the group-link inspector state bit #0204; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0205", "Toggles the SFX-channel note state bit #0205; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0206", "Toggles the account-sync reminder state bit #0206; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0207", "Toggles the prototype flag state bit #0207; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0208", "Toggles the dual-player lane marker state bit #0208; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0209", "Toggles the ship-stability hint state bit #0209; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0210", "Toggles the portal-color marker state bit #0210; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0211", "Toggles the speed-ladder slot state bit #0211; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0212", "Toggles the trigger-chain note state bit #0212; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0213", "Toggles the song-offset checker state bit #0213; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0214", "Toggles the progress-note slot state bit #0214; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0215", "Toggles the future-hook marker state bit #0215; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0216", "Toggles the damage-test latch state bit #0216; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0217", "Toggles the wave-lane monitor state bit #0217; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0218", "Toggles the slope-outline helper state bit #0218; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0219", "Toggles the restart preset state bit #0219; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0220", "Toggles the spawn-order label state bit #0220; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0221", "Toggles the metronome lane state bit #0221; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0222", "Toggles the quest-state card state bit #0222; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0223", "Toggles the diagnostic cell state bit #0223; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0224", "Toggles the spawn-state keeper state bit #0224; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0225", "Toggles the robot-hop timer state bit #0225; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0226", "Toggles the coin-path highlight state bit #0226; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0227", "Toggles the state-cleanup macro state bit #0227; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0228", "Toggles the move-action planner state bit #0228; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0229", "Toggles the sync-point label state bit #0229; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0230", "Toggles the stat-review badge state bit #0230; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0231", "Toggles the compatibility note state bit #0231; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0232", "Toggles the mini-mode tracker state bit #0232; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0233", "Toggles the spider-snap audit state bit #0233; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0234", "Toggles the trigger-pulse badge state bit #0234; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0235", "Toggles the pause-menu shortcut state bit #0235; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0236", "Toggles the color-channel memo state bit #0236; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0237", "Toggles the volume-pass reminder state bit #0237; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0238", "Toggles the profile-route marker state bit #0238; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0239", "Toggles the stress-test latch state bit #0239; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0240", "Toggles the player-speed notebook state bit #0240; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0241", "Toggles the swing-arc sampler state bit #0241; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0242", "Toggles the camera-frame ruler state bit #0242; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0243", "Toggles the timing-note bank state bit #0243; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0244", "Toggles the editor-layer bookmark state bit #0244; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0245", "Toggles the rhythm-window card state bit #0245; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0246", "Toggles the daily-run note state bit #0246; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0247", "Toggles the fallback preset state bit #0247; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0248", "Toggles the safe-practice reminder state bit #0248; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0249", "Toggles the release-safety latch state bit #0249; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0250", "Toggles the contrast-check overlay state bit #0250; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0251", "Toggles the route-bookmark cell state bit #0251; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0252", "Toggles the collision-block checklist state bit #0252; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0253", "Toggles the audio-trigger audit state bit #0253; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0254", "Toggles the reward-check tracker state bit #0254; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0255", "Toggles the integration flag state bit #0255; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0256", "Toggles the icon ghost guard state bit #0256; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0257", "Toggles the tap-window coach state bit #0257; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0258", "Toggles the hitbox-readability layer state bit #0258; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0259", "Toggles the checkpoint workflow state bit #0259; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0260", "Toggles the object-id scratchpad state bit #0260; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0261", "Toggles the beat-grid marker state bit #0261; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0262", "Toggles the achievement checklist state bit #0262; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0263", "Toggles the sandbox switch state bit #0263; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0264", "Toggles the gravity-state watcher state bit #0264; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0265", "Toggles the orb-route planner state bit #0265; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0266", "Toggles the background-noise switch state bit #0266; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0267", "Toggles the attempt-session marker state bit #0267; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0268", "Toggles the group-link inspector state bit #0268; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0269", "Toggles the SFX-channel note state bit #0269; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0270", "Toggles the account-sync reminder state bit #0270; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0271", "Toggles the prototype flag state bit #0271; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0272", "Toggles the dual-player lane marker state bit #0272; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0273", "Toggles the ship-stability hint state bit #0273; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0274", "Toggles the portal-color marker state bit #0274; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0275", "Toggles the speed-ladder slot state bit #0275; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0276", "Toggles the trigger-chain note state bit #0276; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0277", "Toggles the song-offset checker state bit #0277; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0278", "Toggles the progress-note slot state bit #0278; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0279", "Toggles the future-hook marker state bit #0279; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0280", "Toggles the damage-test latch state bit #0280; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0281", "Toggles the wave-lane monitor state bit #0281; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0282", "Toggles the slope-outline helper state bit #0282; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0283", "Toggles the restart preset state bit #0283; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0284", "Toggles the spawn-order label state bit #0284; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0285", "Toggles the metronome lane state bit #0285; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0286", "Toggles the quest-state card state bit #0286; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0287", "Toggles the diagnostic cell state bit #0287; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0288", "Toggles the spawn-state keeper state bit #0288; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0289", "Toggles the robot-hop timer state bit #0289; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0290", "Toggles the coin-path highlight state bit #0290; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0291", "Toggles the state-cleanup macro state bit #0291; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0292", "Toggles the move-action planner state bit #0292; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0293", "Toggles the sync-point label state bit #0293; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0294", "Toggles the stat-review badge state bit #0294; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0295", "Toggles the compatibility note state bit #0295; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0296", "Toggles the mini-mode tracker state bit #0296; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0297", "Toggles the spider-snap audit state bit #0297; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0298", "Toggles the trigger-pulse badge state bit #0298; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0299", "Toggles the pause-menu shortcut state bit #0299; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0300", "Toggles the color-channel memo state bit #0300; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0301", "Toggles the volume-pass reminder state bit #0301; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0302", "Toggles the profile-route marker state bit #0302; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0303", "Toggles the stress-test latch state bit #0303; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0304", "Toggles the player-speed notebook state bit #0304; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0305", "Toggles the swing-arc sampler state bit #0305; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0306", "Toggles the camera-frame ruler state bit #0306; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0307", "Toggles the timing-note bank state bit #0307; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0308", "Toggles the editor-layer bookmark state bit #0308; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0309", "Toggles the rhythm-window card state bit #0309; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0310", "Toggles the daily-run note state bit #0310; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0311", "Toggles the fallback preset state bit #0311; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0312", "Toggles the safe-practice reminder state bit #0312; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0313", "Toggles the release-safety latch state bit #0313; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0314", "Toggles the contrast-check overlay state bit #0314; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0315", "Toggles the route-bookmark cell state bit #0315; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0316", "Toggles the collision-block checklist state bit #0316; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0317", "Toggles the audio-trigger audit state bit #0317; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0318", "Toggles the reward-check tracker state bit #0318; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0319", "Toggles the integration flag state bit #0319; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0320", "Toggles the icon ghost guard state bit #0320; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0321", "Toggles the tap-window coach state bit #0321; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0322", "Toggles the hitbox-readability layer state bit #0322; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0323", "Toggles the checkpoint workflow state bit #0323; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0324", "Toggles the object-id scratchpad state bit #0324; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0325", "Toggles the beat-grid marker state bit #0325; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0326", "Toggles the achievement checklist state bit #0326; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0327", "Toggles the sandbox switch state bit #0327; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0328", "Toggles the gravity-state watcher state bit #0328; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0329", "Toggles the orb-route planner state bit #0329; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0330", "Toggles the background-noise switch state bit #0330; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0331", "Toggles the attempt-session marker state bit #0331; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0332", "Toggles the group-link inspector state bit #0332; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0333", "Toggles the SFX-channel note state bit #0333; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0334", "Toggles the account-sync reminder state bit #0334; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0335", "Toggles the prototype flag state bit #0335; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0336", "Toggles the dual-player lane marker state bit #0336; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0337", "Toggles the ship-stability hint state bit #0337; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0338", "Toggles the portal-color marker state bit #0338; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0339", "Toggles the speed-ladder slot state bit #0339; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0340", "Toggles the trigger-chain note state bit #0340; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0341", "Toggles the song-offset checker state bit #0341; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0342", "Toggles the progress-note slot state bit #0342; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0343", "Toggles the future-hook marker state bit #0343; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0344", "Toggles the damage-test latch state bit #0344; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0345", "Toggles the wave-lane monitor state bit #0345; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0346", "Toggles the slope-outline helper state bit #0346; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0347", "Toggles the restart preset state bit #0347; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0348", "Toggles the spawn-order label state bit #0348; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0349", "Toggles the metronome lane state bit #0349; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0350", "Toggles the quest-state card state bit #0350; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0351", "Toggles the diagnostic cell state bit #0351; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0352", "Toggles the spawn-state keeper state bit #0352; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0353", "Toggles the robot-hop timer state bit #0353; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0354", "Toggles the coin-path highlight state bit #0354; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0355", "Toggles the state-cleanup macro state bit #0355; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0356", "Toggles the move-action planner state bit #0356; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0357", "Toggles the sync-point label state bit #0357; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0358", "Toggles the stat-review badge state bit #0358; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0359", "Toggles the compatibility note state bit #0359; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0360", "Toggles the mini-mode tracker state bit #0360; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0361", "Toggles the spider-snap audit state bit #0361; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0362", "Toggles the trigger-pulse badge state bit #0362; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0363", "Toggles the pause-menu shortcut state bit #0363; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0364", "Toggles the color-channel memo state bit #0364; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0365", "Toggles the volume-pass reminder state bit #0365; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0366", "Toggles the profile-route marker state bit #0366; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0367", "Toggles the stress-test latch state bit #0367; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0368", "Toggles the player-speed notebook state bit #0368; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0369", "Toggles the swing-arc sampler state bit #0369; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0370", "Toggles the camera-frame ruler state bit #0370; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0371", "Toggles the timing-note bank state bit #0371; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0372", "Toggles the editor-layer bookmark state bit #0372; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0373", "Toggles the rhythm-window card state bit #0373; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0374", "Toggles the daily-run note state bit #0374; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0375", "Toggles the fallback preset state bit #0375; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0376", "Toggles the safe-practice reminder state bit #0376; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0377", "Toggles the release-safety latch state bit #0377; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0378", "Toggles the contrast-check overlay state bit #0378; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0379", "Toggles the route-bookmark cell state bit #0379; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0380", "Toggles the collision-block checklist state bit #0380; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0381", "Toggles the audio-trigger audit state bit #0381; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0382", "Toggles the reward-check tracker state bit #0382; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0383", "Toggles the integration flag state bit #0383; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0384", "Toggles the icon ghost guard state bit #0384; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0385", "Toggles the tap-window coach state bit #0385; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0386", "Toggles the hitbox-readability layer state bit #0386; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0387", "Toggles the checkpoint workflow state bit #0387; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0388", "Toggles the object-id scratchpad state bit #0388; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0389", "Toggles the beat-grid marker state bit #0389; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0390", "Toggles the achievement checklist state bit #0390; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0391", "Toggles the sandbox switch state bit #0391; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0392", "Toggles the gravity-state watcher state bit #0392; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0393", "Toggles the orb-route planner state bit #0393; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0394", "Toggles the background-noise switch state bit #0394; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0395", "Toggles the attempt-session marker state bit #0395; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0396", "Toggles the group-link inspector state bit #0396; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0397", "Toggles the SFX-channel note state bit #0397; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0398", "Toggles the account-sync reminder state bit #0398; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0399", "Toggles the prototype flag state bit #0399; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0400", "Toggles the dual-player lane marker state bit #0400; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0401", "Toggles the ship-stability hint state bit #0401; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0402", "Toggles the portal-color marker state bit #0402; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0403", "Toggles the speed-ladder slot state bit #0403; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0404", "Toggles the trigger-chain note state bit #0404; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0405", "Toggles the song-offset checker state bit #0405; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0406", "Toggles the progress-note slot state bit #0406; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0407", "Toggles the future-hook marker state bit #0407; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0408", "Toggles the damage-test latch state bit #0408; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0409", "Toggles the wave-lane monitor state bit #0409; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0410", "Toggles the slope-outline helper state bit #0410; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0411", "Toggles the restart preset state bit #0411; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0412", "Toggles the spawn-order label state bit #0412; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0413", "Toggles the metronome lane state bit #0413; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0414", "Toggles the quest-state card state bit #0414; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0415", "Toggles the diagnostic cell state bit #0415; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0416", "Toggles the spawn-state keeper state bit #0416; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0417", "Toggles the robot-hop timer state bit #0417; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0418", "Toggles the coin-path highlight state bit #0418; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0419", "Toggles the state-cleanup macro state bit #0419; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0420", "Toggles the move-action planner state bit #0420; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0421", "Toggles the sync-point label state bit #0421; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0422", "Toggles the stat-review badge state bit #0422; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0423", "Toggles the compatibility note state bit #0423; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0424", "Toggles the mini-mode tracker state bit #0424; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0425", "Toggles the spider-snap audit state bit #0425; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0426", "Toggles the trigger-pulse badge state bit #0426; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0427", "Toggles the pause-menu shortcut state bit #0427; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0428", "Toggles the color-channel memo state bit #0428; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0429", "Toggles the volume-pass reminder state bit #0429; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0430", "Toggles the profile-route marker state bit #0430; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0431", "Toggles the stress-test latch state bit #0431; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0432", "Toggles the player-speed notebook state bit #0432; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0433", "Toggles the swing-arc sampler state bit #0433; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0434", "Toggles the camera-frame ruler state bit #0434; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0435", "Toggles the timing-note bank state bit #0435; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0436", "Toggles the editor-layer bookmark state bit #0436; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0437", "Toggles the rhythm-window card state bit #0437; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0438", "Toggles the daily-run note state bit #0438; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0439", "Toggles the fallback preset state bit #0439; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0440", "Toggles the safe-practice reminder state bit #0440; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0441", "Toggles the release-safety latch state bit #0441; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0442", "Toggles the contrast-check overlay state bit #0442; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0443", "Toggles the route-bookmark cell state bit #0443; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0444", "Toggles the collision-block checklist state bit #0444; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0445", "Toggles the audio-trigger audit state bit #0445; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0446", "Toggles the reward-check tracker state bit #0446; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0447", "Toggles the integration flag state bit #0447; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0448", "Toggles the icon ghost guard state bit #0448; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0449", "Toggles the tap-window coach state bit #0449; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0450", "Toggles the hitbox-readability layer state bit #0450; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0451", "Toggles the checkpoint workflow state bit #0451; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0452", "Toggles the object-id scratchpad state bit #0452; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0453", "Toggles the beat-grid marker state bit #0453; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0454", "Toggles the achievement checklist state bit #0454; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0455", "Toggles the sandbox switch state bit #0455; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0456", "Toggles the gravity-state watcher state bit #0456; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0457", "Toggles the orb-route planner state bit #0457; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0458", "Toggles the background-noise switch state bit #0458; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0459", "Toggles the attempt-session marker state bit #0459; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0460", "Toggles the group-link inspector state bit #0460; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0461", "Toggles the SFX-channel note state bit #0461; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0462", "Toggles the account-sync reminder state bit #0462; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0463", "Toggles the prototype flag state bit #0463; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0464", "Toggles the dual-player lane marker state bit #0464; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0465", "Toggles the ship-stability hint state bit #0465; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0466", "Toggles the portal-color marker state bit #0466; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0467", "Toggles the speed-ladder slot state bit #0467; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0468", "Toggles the trigger-chain note state bit #0468; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0469", "Toggles the song-offset checker state bit #0469; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0470", "Toggles the progress-note slot state bit #0470; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0471", "Toggles the future-hook marker state bit #0471; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0472", "Toggles the damage-test latch state bit #0472; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0473", "Toggles the wave-lane monitor state bit #0473; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0474", "Toggles the slope-outline helper state bit #0474; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0475", "Toggles the restart preset state bit #0475; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0476", "Toggles the spawn-order label state bit #0476; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0477", "Toggles the metronome lane state bit #0477; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0478", "Toggles the quest-state card state bit #0478; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0479", "Toggles the diagnostic cell state bit #0479; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0480", "Toggles the spawn-state keeper state bit #0480; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0481", "Toggles the robot-hop timer state bit #0481; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0482", "Toggles the coin-path highlight state bit #0482; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0483", "Toggles the state-cleanup macro state bit #0483; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0484", "Toggles the move-action planner state bit #0484; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0485", "Toggles the sync-point label state bit #0485; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0486", "Toggles the stat-review badge state bit #0486; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0487", "Toggles the compatibility note state bit #0487; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0488", "Toggles the mini-mode tracker state bit #0488; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0489", "Toggles the spider-snap audit state bit #0489; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0490", "Toggles the trigger-pulse badge state bit #0490; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0491", "Toggles the pause-menu shortcut state bit #0491; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0492", "Toggles the color-channel memo state bit #0492; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0493", "Toggles the volume-pass reminder state bit #0493; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0494", "Toggles the profile-route marker state bit #0494; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0495", "Toggles the stress-test latch state bit #0495; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0496", "Toggles the player-speed notebook state bit #0496; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0497", "Toggles the swing-arc sampler state bit #0497; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0498", "Toggles the camera-frame ruler state bit #0498; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0499", "Toggles the timing-note bank state bit #0499; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0500", "Toggles the editor-layer bookmark state bit #0500; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0501", "Toggles the rhythm-window card state bit #0501; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0502", "Toggles the daily-run note state bit #0502; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0503", "Toggles the fallback preset state bit #0503; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0504", "Toggles the safe-practice reminder state bit #0504; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0505", "Toggles the release-safety latch state bit #0505; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0506", "Toggles the contrast-check overlay state bit #0506; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0507", "Toggles the route-bookmark cell state bit #0507; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0508", "Toggles the collision-block checklist state bit #0508; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0509", "Toggles the audio-trigger audit state bit #0509; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0510", "Toggles the reward-check tracker state bit #0510; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0511", "Toggles the integration flag state bit #0511; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0512", "Toggles the icon ghost guard state bit #0512; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0513", "Toggles the tap-window coach state bit #0513; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0514", "Toggles the hitbox-readability layer state bit #0514; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0515", "Toggles the checkpoint workflow state bit #0515; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0516", "Toggles the object-id scratchpad state bit #0516; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0517", "Toggles the beat-grid marker state bit #0517; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0518", "Toggles the achievement checklist state bit #0518; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0519", "Toggles the sandbox switch state bit #0519; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0520", "Toggles the gravity-state watcher state bit #0520; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0521", "Toggles the orb-route planner state bit #0521; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0522", "Toggles the background-noise switch state bit #0522; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0523", "Toggles the attempt-session marker state bit #0523; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0524", "Toggles the group-link inspector state bit #0524; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0525", "Toggles the SFX-channel note state bit #0525; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0526", "Toggles the account-sync reminder state bit #0526; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0527", "Toggles the prototype flag state bit #0527; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0528", "Toggles the dual-player lane marker state bit #0528; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0529", "Toggles the ship-stability hint state bit #0529; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0530", "Toggles the portal-color marker state bit #0530; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0531", "Toggles the speed-ladder slot state bit #0531; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0532", "Toggles the trigger-chain note state bit #0532; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0533", "Toggles the song-offset checker state bit #0533; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0534", "Toggles the progress-note slot state bit #0534; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0535", "Toggles the future-hook marker state bit #0535; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0536", "Toggles the damage-test latch state bit #0536; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0537", "Toggles the wave-lane monitor state bit #0537; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0538", "Toggles the slope-outline helper state bit #0538; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0539", "Toggles the restart preset state bit #0539; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0540", "Toggles the spawn-order label state bit #0540; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0541", "Toggles the metronome lane state bit #0541; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0542", "Toggles the quest-state card state bit #0542; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0543", "Toggles the diagnostic cell state bit #0543; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0544", "Toggles the spawn-state keeper state bit #0544; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0545", "Toggles the robot-hop timer state bit #0545; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0546", "Toggles the coin-path highlight state bit #0546; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0547", "Toggles the state-cleanup macro state bit #0547; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0548", "Toggles the move-action planner state bit #0548; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0549", "Toggles the sync-point label state bit #0549; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0550", "Toggles the stat-review badge state bit #0550; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0551", "Toggles the compatibility note state bit #0551; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0552", "Toggles the mini-mode tracker state bit #0552; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0553", "Toggles the spider-snap audit state bit #0553; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0554", "Toggles the trigger-pulse badge state bit #0554; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0555", "Toggles the pause-menu shortcut state bit #0555; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0556", "Toggles the color-channel memo state bit #0556; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0557", "Toggles the volume-pass reminder state bit #0557; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0558", "Toggles the profile-route marker state bit #0558; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0559", "Toggles the stress-test latch state bit #0559; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0560", "Toggles the player-speed notebook state bit #0560; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0561", "Toggles the swing-arc sampler state bit #0561; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0562", "Toggles the camera-frame ruler state bit #0562; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0563", "Toggles the timing-note bank state bit #0563; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0564", "Toggles the editor-layer bookmark state bit #0564; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0565", "Toggles the rhythm-window card state bit #0565; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0566", "Toggles the daily-run note state bit #0566; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0567", "Toggles the fallback preset state bit #0567; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0568", "Toggles the safe-practice reminder state bit #0568; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0569", "Toggles the release-safety latch state bit #0569; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0570", "Toggles the contrast-check overlay state bit #0570; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0571", "Toggles the route-bookmark cell state bit #0571; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0572", "Toggles the collision-block checklist state bit #0572; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0573", "Toggles the audio-trigger audit state bit #0573; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0574", "Toggles the reward-check tracker state bit #0574; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0575", "Toggles the integration flag state bit #0575; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0576", "Toggles the icon ghost guard state bit #0576; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0577", "Toggles the tap-window coach state bit #0577; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0578", "Toggles the hitbox-readability layer state bit #0578; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0579", "Toggles the checkpoint workflow state bit #0579; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0580", "Toggles the object-id scratchpad state bit #0580; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0581", "Toggles the beat-grid marker state bit #0581; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0582", "Toggles the achievement checklist state bit #0582; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0583", "Toggles the sandbox switch state bit #0583; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0584", "Toggles the gravity-state watcher state bit #0584; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0585", "Toggles the orb-route planner state bit #0585; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0586", "Toggles the background-noise switch state bit #0586; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0587", "Toggles the attempt-session marker state bit #0587; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0588", "Toggles the group-link inspector state bit #0588; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0589", "Toggles the SFX-channel note state bit #0589; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0590", "Toggles the account-sync reminder state bit #0590; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0591", "Toggles the prototype flag state bit #0591; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0592", "Toggles the dual-player lane marker state bit #0592; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0593", "Toggles the ship-stability hint state bit #0593; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0594", "Toggles the portal-color marker state bit #0594; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0595", "Toggles the speed-ladder slot state bit #0595; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0596", "Toggles the trigger-chain note state bit #0596; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0597", "Toggles the song-offset checker state bit #0597; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0598", "Toggles the progress-note slot state bit #0598; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0599", "Toggles the future-hook marker state bit #0599; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0600", "Toggles the damage-test latch state bit #0600; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0601", "Toggles the wave-lane monitor state bit #0601; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0602", "Toggles the slope-outline helper state bit #0602; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0603", "Toggles the restart preset state bit #0603; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0604", "Toggles the spawn-order label state bit #0604; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0605", "Toggles the metronome lane state bit #0605; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0606", "Toggles the quest-state card state bit #0606; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0607", "Toggles the diagnostic cell state bit #0607; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0608", "Toggles the spawn-state keeper state bit #0608; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0609", "Toggles the robot-hop timer state bit #0609; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0610", "Toggles the coin-path highlight state bit #0610; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0611", "Toggles the state-cleanup macro state bit #0611; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0612", "Toggles the move-action planner state bit #0612; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0613", "Toggles the sync-point label state bit #0613; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0614", "Toggles the stat-review badge state bit #0614; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0615", "Toggles the compatibility note state bit #0615; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0616", "Toggles the mini-mode tracker state bit #0616; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0617", "Toggles the spider-snap audit state bit #0617; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0618", "Toggles the trigger-pulse badge state bit #0618; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0619", "Toggles the pause-menu shortcut state bit #0619; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0620", "Toggles the color-channel memo state bit #0620; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0621", "Toggles the volume-pass reminder state bit #0621; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0622", "Toggles the profile-route marker state bit #0622; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0623", "Toggles the stress-test latch state bit #0623; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0624", "Toggles the player-speed notebook state bit #0624; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0625", "Toggles the swing-arc sampler state bit #0625; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0626", "Toggles the camera-frame ruler state bit #0626; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0627", "Toggles the timing-note bank state bit #0627; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0628", "Toggles the editor-layer bookmark state bit #0628; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0629", "Toggles the rhythm-window card state bit #0629; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0630", "Toggles the daily-run note state bit #0630; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0631", "Toggles the fallback preset state bit #0631; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0632", "Toggles the safe-practice reminder state bit #0632; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0633", "Toggles the release-safety latch state bit #0633; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0634", "Toggles the contrast-check overlay state bit #0634; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0635", "Toggles the route-bookmark cell state bit #0635; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0636", "Toggles the collision-block checklist state bit #0636; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0637", "Toggles the audio-trigger audit state bit #0637; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0638", "Toggles the reward-check tracker state bit #0638; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0639", "Toggles the integration flag state bit #0639; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0640", "Toggles the icon ghost guard state bit #0640; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0641", "Toggles the tap-window coach state bit #0641; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0642", "Toggles the hitbox-readability layer state bit #0642; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0643", "Toggles the checkpoint workflow state bit #0643; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0644", "Toggles the object-id scratchpad state bit #0644; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0645", "Toggles the beat-grid marker state bit #0645; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0646", "Toggles the achievement checklist state bit #0646; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0647", "Toggles the sandbox switch state bit #0647; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0648", "Toggles the gravity-state watcher state bit #0648; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0649", "Toggles the orb-route planner state bit #0649; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0650", "Toggles the background-noise switch state bit #0650; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0651", "Toggles the attempt-session marker state bit #0651; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0652", "Toggles the group-link inspector state bit #0652; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0653", "Toggles the SFX-channel note state bit #0653; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0654", "Toggles the account-sync reminder state bit #0654; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0655", "Toggles the prototype flag state bit #0655; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0656", "Toggles the dual-player lane marker state bit #0656; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0657", "Toggles the ship-stability hint state bit #0657; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0658", "Toggles the portal-color marker state bit #0658; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0659", "Toggles the speed-ladder slot state bit #0659; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0660", "Toggles the trigger-chain note state bit #0660; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0661", "Toggles the song-offset checker state bit #0661; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0662", "Toggles the progress-note slot state bit #0662; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0663", "Toggles the future-hook marker state bit #0663; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0664", "Toggles the damage-test latch state bit #0664; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0665", "Toggles the wave-lane monitor state bit #0665; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0666", "Toggles the slope-outline helper state bit #0666; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0667", "Toggles the restart preset state bit #0667; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0668", "Toggles the spawn-order label state bit #0668; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0669", "Toggles the metronome lane state bit #0669; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0670", "Toggles the quest-state card state bit #0670; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0671", "Toggles the diagnostic cell state bit #0671; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0672", "Toggles the spawn-state keeper state bit #0672; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0673", "Toggles the robot-hop timer state bit #0673; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0674", "Toggles the coin-path highlight state bit #0674; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0675", "Toggles the state-cleanup macro state bit #0675; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0676", "Toggles the move-action planner state bit #0676; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0677", "Toggles the sync-point label state bit #0677; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0678", "Toggles the stat-review badge state bit #0678; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0679", "Toggles the compatibility note state bit #0679; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0680", "Toggles the mini-mode tracker state bit #0680; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0681", "Toggles the spider-snap audit state bit #0681; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0682", "Toggles the trigger-pulse badge state bit #0682; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0683", "Toggles the pause-menu shortcut state bit #0683; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0684", "Toggles the color-channel memo state bit #0684; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0685", "Toggles the volume-pass reminder state bit #0685; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0686", "Toggles the profile-route marker state bit #0686; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0687", "Toggles the stress-test latch state bit #0687; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0688", "Toggles the player-speed notebook state bit #0688; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0689", "Toggles the swing-arc sampler state bit #0689; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0690", "Toggles the camera-frame ruler state bit #0690; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0691", "Toggles the timing-note bank state bit #0691; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0692", "Toggles the editor-layer bookmark state bit #0692; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0693", "Toggles the rhythm-window card state bit #0693; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0694", "Toggles the daily-run note state bit #0694; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0695", "Toggles the fallback preset state bit #0695; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0696", "Toggles the safe-practice reminder state bit #0696; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0697", "Toggles the release-safety latch state bit #0697; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0698", "Toggles the contrast-check overlay state bit #0698; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0699", "Toggles the route-bookmark cell state bit #0699; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0700", "Toggles the collision-block checklist state bit #0700; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0701", "Toggles the audio-trigger audit state bit #0701; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0702", "Toggles the reward-check tracker state bit #0702; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0703", "Toggles the integration flag state bit #0703; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0704", "Toggles the icon ghost guard state bit #0704; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0705", "Toggles the tap-window coach state bit #0705; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0706", "Toggles the hitbox-readability layer state bit #0706; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0707", "Toggles the checkpoint workflow state bit #0707; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0708", "Toggles the object-id scratchpad state bit #0708; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0709", "Toggles the beat-grid marker state bit #0709; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0710", "Toggles the achievement checklist state bit #0710; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0711", "Toggles the sandbox switch state bit #0711; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0712", "Toggles the gravity-state watcher state bit #0712; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0713", "Toggles the orb-route planner state bit #0713; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0714", "Toggles the background-noise switch state bit #0714; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0715", "Toggles the attempt-session marker state bit #0715; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0716", "Toggles the group-link inspector state bit #0716; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0717", "Toggles the SFX-channel note state bit #0717; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0718", "Toggles the account-sync reminder state bit #0718; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0719", "Toggles the prototype flag state bit #0719; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0720", "Toggles the dual-player lane marker state bit #0720; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0721", "Toggles the ship-stability hint state bit #0721; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0722", "Toggles the portal-color marker state bit #0722; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0723", "Toggles the speed-ladder slot state bit #0723; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0724", "Toggles the trigger-chain note state bit #0724; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0725", "Toggles the song-offset checker state bit #0725; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0726", "Toggles the progress-note slot state bit #0726; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0727", "Toggles the future-hook marker state bit #0727; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0728", "Toggles the damage-test latch state bit #0728; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0729", "Toggles the wave-lane monitor state bit #0729; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0730", "Toggles the slope-outline helper state bit #0730; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0731", "Toggles the restart preset state bit #0731; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0732", "Toggles the spawn-order label state bit #0732; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0733", "Toggles the metronome lane state bit #0733; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0734", "Toggles the quest-state card state bit #0734; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0735", "Toggles the diagnostic cell state bit #0735; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0736", "Toggles the spawn-state keeper state bit #0736; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0737", "Toggles the robot-hop timer state bit #0737; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0738", "Toggles the coin-path highlight state bit #0738; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0739", "Toggles the state-cleanup macro state bit #0739; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0740", "Toggles the move-action planner state bit #0740; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0741", "Toggles the sync-point label state bit #0741; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0742", "Toggles the stat-review badge state bit #0742; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0743", "Toggles the compatibility note state bit #0743; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0744", "Toggles the mini-mode tracker state bit #0744; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0745", "Toggles the spider-snap audit state bit #0745; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0746", "Toggles the trigger-pulse badge state bit #0746; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0747", "Toggles the pause-menu shortcut state bit #0747; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0748", "Toggles the color-channel memo state bit #0748; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0749", "Toggles the volume-pass reminder state bit #0749; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0750", "Toggles the profile-route marker state bit #0750; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0751", "Toggles the stress-test latch state bit #0751; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0752", "Toggles the player-speed notebook state bit #0752; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0753", "Toggles the swing-arc sampler state bit #0753; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0754", "Toggles the camera-frame ruler state bit #0754; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0755", "Toggles the timing-note bank state bit #0755; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0756", "Toggles the editor-layer bookmark state bit #0756; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0757", "Toggles the rhythm-window card state bit #0757; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0758", "Toggles the daily-run note state bit #0758; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0759", "Toggles the fallback preset state bit #0759; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0760", "Toggles the safe-practice reminder state bit #0760; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0761", "Toggles the release-safety latch state bit #0761; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0762", "Toggles the contrast-check overlay state bit #0762; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0763", "Toggles the route-bookmark cell state bit #0763; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0764", "Toggles the collision-block checklist state bit #0764; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0765", "Toggles the audio-trigger audit state bit #0765; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0766", "Toggles the reward-check tracker state bit #0766; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0767", "Toggles the integration flag state bit #0767; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0768", "Toggles the icon ghost guard state bit #0768; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0769", "Toggles the tap-window coach state bit #0769; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0770", "Toggles the hitbox-readability layer state bit #0770; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0771", "Toggles the checkpoint workflow state bit #0771; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0772", "Toggles the object-id scratchpad state bit #0772; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0773", "Toggles the beat-grid marker state bit #0773; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0774", "Toggles the achievement checklist state bit #0774; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0775", "Toggles the sandbox switch state bit #0775; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0776", "Toggles the gravity-state watcher state bit #0776; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0777", "Toggles the orb-route planner state bit #0777; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0778", "Toggles the background-noise switch state bit #0778; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0779", "Toggles the attempt-session marker state bit #0779; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0780", "Toggles the group-link inspector state bit #0780; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0781", "Toggles the SFX-channel note state bit #0781; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0782", "Toggles the account-sync reminder state bit #0782; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0783", "Toggles the prototype flag state bit #0783; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0784", "Toggles the dual-player lane marker state bit #0784; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0785", "Toggles the ship-stability hint state bit #0785; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0786", "Toggles the portal-color marker state bit #0786; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0787", "Toggles the speed-ladder slot state bit #0787; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0788", "Toggles the trigger-chain note state bit #0788; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0789", "Toggles the song-offset checker state bit #0789; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0790", "Toggles the progress-note slot state bit #0790; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0791", "Toggles the future-hook marker state bit #0791; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0792", "Toggles the damage-test latch state bit #0792; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0793", "Toggles the wave-lane monitor state bit #0793; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0794", "Toggles the slope-outline helper state bit #0794; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0795", "Toggles the restart preset state bit #0795; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0796", "Toggles the spawn-order label state bit #0796; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0797", "Toggles the metronome lane state bit #0797; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0798", "Toggles the quest-state card state bit #0798; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0799", "Toggles the diagnostic cell state bit #0799; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0800", "Toggles the spawn-state keeper state bit #0800; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0801", "Toggles the robot-hop timer state bit #0801; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0802", "Toggles the coin-path highlight state bit #0802; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0803", "Toggles the state-cleanup macro state bit #0803; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0804", "Toggles the move-action planner state bit #0804; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0805", "Toggles the sync-point label state bit #0805; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0806", "Toggles the stat-review badge state bit #0806; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0807", "Toggles the compatibility note state bit #0807; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0808", "Toggles the mini-mode tracker state bit #0808; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0809", "Toggles the spider-snap audit state bit #0809; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0810", "Toggles the trigger-pulse badge state bit #0810; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0811", "Toggles the pause-menu shortcut state bit #0811; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0812", "Toggles the color-channel memo state bit #0812; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0813", "Toggles the volume-pass reminder state bit #0813; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0814", "Toggles the profile-route marker state bit #0814; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0815", "Toggles the stress-test latch state bit #0815; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0816", "Toggles the player-speed notebook state bit #0816; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0817", "Toggles the swing-arc sampler state bit #0817; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0818", "Toggles the camera-frame ruler state bit #0818; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0819", "Toggles the timing-note bank state bit #0819; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0820", "Toggles the editor-layer bookmark state bit #0820; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0821", "Toggles the rhythm-window card state bit #0821; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0822", "Toggles the daily-run note state bit #0822; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0823", "Toggles the fallback preset state bit #0823; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0824", "Toggles the safe-practice reminder state bit #0824; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0825", "Toggles the release-safety latch state bit #0825; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0826", "Toggles the contrast-check overlay state bit #0826; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0827", "Toggles the route-bookmark cell state bit #0827; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0828", "Toggles the collision-block checklist state bit #0828; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0829", "Toggles the audio-trigger audit state bit #0829; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0830", "Toggles the reward-check tracker state bit #0830; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0831", "Toggles the integration flag state bit #0831; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0832", "Toggles the icon ghost guard state bit #0832; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0833", "Toggles the tap-window coach state bit #0833; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0834", "Toggles the hitbox-readability layer state bit #0834; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0835", "Toggles the checkpoint workflow state bit #0835; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0836", "Toggles the object-id scratchpad state bit #0836; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0837", "Toggles the beat-grid marker state bit #0837; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0838", "Toggles the achievement checklist state bit #0838; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0839", "Toggles the sandbox switch state bit #0839; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0840", "Toggles the gravity-state watcher state bit #0840; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0841", "Toggles the orb-route planner state bit #0841; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0842", "Toggles the background-noise switch state bit #0842; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0843", "Toggles the attempt-session marker state bit #0843; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0844", "Toggles the group-link inspector state bit #0844; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0845", "Toggles the SFX-channel note state bit #0845; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0846", "Toggles the account-sync reminder state bit #0846; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0847", "Toggles the prototype flag state bit #0847; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0848", "Toggles the dual-player lane marker state bit #0848; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0849", "Toggles the ship-stability hint state bit #0849; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0850", "Toggles the portal-color marker state bit #0850; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0851", "Toggles the speed-ladder slot state bit #0851; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0852", "Toggles the trigger-chain note state bit #0852; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0853", "Toggles the song-offset checker state bit #0853; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0854", "Toggles the progress-note slot state bit #0854; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0855", "Toggles the future-hook marker state bit #0855; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0856", "Toggles the damage-test latch state bit #0856; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0857", "Toggles the wave-lane monitor state bit #0857; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0858", "Toggles the slope-outline helper state bit #0858; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0859", "Toggles the restart preset state bit #0859; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0860", "Toggles the spawn-order label state bit #0860; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0861", "Toggles the metronome lane state bit #0861; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0862", "Toggles the quest-state card state bit #0862; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0863", "Toggles the diagnostic cell state bit #0863; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0864", "Toggles the spawn-state keeper state bit #0864; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0865", "Toggles the robot-hop timer state bit #0865; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0866", "Toggles the coin-path highlight state bit #0866; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0867", "Toggles the state-cleanup macro state bit #0867; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0868", "Toggles the move-action planner state bit #0868; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0869", "Toggles the sync-point label state bit #0869; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0870", "Toggles the stat-review badge state bit #0870; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0871", "Toggles the compatibility note state bit #0871; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0872", "Toggles the mini-mode tracker state bit #0872; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0873", "Toggles the spider-snap audit state bit #0873; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0874", "Toggles the trigger-pulse badge state bit #0874; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0875", "Toggles the pause-menu shortcut state bit #0875; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0876", "Toggles the color-channel memo state bit #0876; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0877", "Toggles the volume-pass reminder state bit #0877; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0878", "Toggles the profile-route marker state bit #0878; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0879", "Toggles the stress-test latch state bit #0879; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0880", "Toggles the player-speed notebook state bit #0880; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0881", "Toggles the swing-arc sampler state bit #0881; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0882", "Toggles the camera-frame ruler state bit #0882; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0883", "Toggles the timing-note bank state bit #0883; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0884", "Toggles the editor-layer bookmark state bit #0884; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0885", "Toggles the rhythm-window card state bit #0885; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0886", "Toggles the daily-run note state bit #0886; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0887", "Toggles the fallback preset state bit #0887; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0888", "Toggles the safe-practice reminder state bit #0888; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0889", "Toggles the release-safety latch state bit #0889; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0890", "Toggles the contrast-check overlay state bit #0890; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0891", "Toggles the route-bookmark cell state bit #0891; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0892", "Toggles the collision-block checklist state bit #0892; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0893", "Toggles the audio-trigger audit state bit #0893; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0894", "Toggles the reward-check tracker state bit #0894; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0895", "Toggles the integration flag state bit #0895; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0896", "Toggles the icon ghost guard state bit #0896; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0897", "Toggles the tap-window coach state bit #0897; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0898", "Toggles the hitbox-readability layer state bit #0898; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0899", "Toggles the checkpoint workflow state bit #0899; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0900", "Toggles the object-id scratchpad state bit #0900; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0901", "Toggles the beat-grid marker state bit #0901; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0902", "Toggles the achievement checklist state bit #0902; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0903", "Toggles the sandbox switch state bit #0903; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0904", "Toggles the gravity-state watcher state bit #0904; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0905", "Toggles the orb-route planner state bit #0905; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0906", "Toggles the background-noise switch state bit #0906; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0907", "Toggles the attempt-session marker state bit #0907; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0908", "Toggles the group-link inspector state bit #0908; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0909", "Toggles the SFX-channel note state bit #0909; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0910", "Toggles the account-sync reminder state bit #0910; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0911", "Toggles the prototype flag state bit #0911; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0912", "Toggles the dual-player lane marker state bit #0912; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0913", "Toggles the ship-stability hint state bit #0913; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0914", "Toggles the portal-color marker state bit #0914; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0915", "Toggles the speed-ladder slot state bit #0915; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0916", "Toggles the trigger-chain note state bit #0916; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0917", "Toggles the song-offset checker state bit #0917; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0918", "Toggles the progress-note slot state bit #0918; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0919", "Toggles the future-hook marker state bit #0919; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0920", "Toggles the damage-test latch state bit #0920; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0921", "Toggles the wave-lane monitor state bit #0921; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0922", "Toggles the slope-outline helper state bit #0922; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0923", "Toggles the restart preset state bit #0923; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0924", "Toggles the spawn-order label state bit #0924; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0925", "Toggles the metronome lane state bit #0925; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0926", "Toggles the quest-state card state bit #0926; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0927", "Toggles the diagnostic cell state bit #0927; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0928", "Toggles the spawn-state keeper state bit #0928; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0929", "Toggles the robot-hop timer state bit #0929; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0930", "Toggles the coin-path highlight state bit #0930; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0931", "Toggles the state-cleanup macro state bit #0931; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0932", "Toggles the move-action planner state bit #0932; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0933", "Toggles the sync-point label state bit #0933; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0934", "Toggles the stat-review badge state bit #0934; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0935", "Toggles the compatibility note state bit #0935; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 0936", "Toggles the mini-mode tracker state bit #0936; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 0937", "Toggles the spider-snap audit state bit #0937; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 0938", "Toggles the trigger-pulse badge state bit #0938; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 0939", "Toggles the pause-menu shortcut state bit #0939; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 0940", "Toggles the color-channel memo state bit #0940; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 0941", "Toggles the volume-pass reminder state bit #0941; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 0942", "Toggles the profile-route marker state bit #0942; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 0943", "Toggles the stress-test latch state bit #0943; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 0944", "Toggles the player-speed notebook state bit #0944; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 0945", "Toggles the swing-arc sampler state bit #0945; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 0946", "Toggles the camera-frame ruler state bit #0946; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 0947", "Toggles the timing-note bank state bit #0947; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 0948", "Toggles the editor-layer bookmark state bit #0948; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 0949", "Toggles the rhythm-window card state bit #0949; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 0950", "Toggles the daily-run note state bit #0950; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 0951", "Toggles the fallback preset state bit #0951; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 0952", "Toggles the safe-practice reminder state bit #0952; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 0953", "Toggles the release-safety latch state bit #0953; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 0954", "Toggles the contrast-check overlay state bit #0954; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 0955", "Toggles the route-bookmark cell state bit #0955; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 0956", "Toggles the collision-block checklist state bit #0956; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 0957", "Toggles the audio-trigger audit state bit #0957; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 0958", "Toggles the reward-check tracker state bit #0958; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 0959", "Toggles the integration flag state bit #0959; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 0960", "Toggles the icon ghost guard state bit #0960; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 0961", "Toggles the tap-window coach state bit #0961; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 0962", "Toggles the hitbox-readability layer state bit #0962; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 0963", "Toggles the checkpoint workflow state bit #0963; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 0964", "Toggles the object-id scratchpad state bit #0964; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 0965", "Toggles the beat-grid marker state bit #0965; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 0966", "Toggles the achievement checklist state bit #0966; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 0967", "Toggles the sandbox switch state bit #0967; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 0968", "Toggles the gravity-state watcher state bit #0968; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 0969", "Toggles the orb-route planner state bit #0969; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 0970", "Toggles the background-noise switch state bit #0970; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 0971", "Toggles the attempt-session marker state bit #0971; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 0972", "Toggles the group-link inspector state bit #0972; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 0973", "Toggles the SFX-channel note state bit #0973; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 0974", "Toggles the account-sync reminder state bit #0974; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 0975", "Toggles the prototype flag state bit #0975; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 0976", "Toggles the dual-player lane marker state bit #0976; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 0977", "Toggles the ship-stability hint state bit #0977; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 0978", "Toggles the portal-color marker state bit #0978; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 0979", "Toggles the speed-ladder slot state bit #0979; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 0980", "Toggles the trigger-chain note state bit #0980; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 0981", "Toggles the song-offset checker state bit #0981; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 0982", "Toggles the progress-note slot state bit #0982; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 0983", "Toggles the future-hook marker state bit #0983; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 0984", "Toggles the damage-test latch state bit #0984; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 0985", "Toggles the wave-lane monitor state bit #0985; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 0986", "Toggles the slope-outline helper state bit #0986; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 0987", "Toggles the restart preset state bit #0987; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 0988", "Toggles the spawn-order label state bit #0988; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 0989", "Toggles the metronome lane state bit #0989; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 0990", "Toggles the quest-state card state bit #0990; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 0991", "Toggles the diagnostic cell state bit #0991; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 0992", "Toggles the spawn-state keeper state bit #0992; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 0993", "Toggles the robot-hop timer state bit #0993; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 0994", "Toggles the coin-path highlight state bit #0994; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 0995", "Toggles the state-cleanup macro state bit #0995; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 0996", "Toggles the move-action planner state bit #0996; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 0997", "Toggles the sync-point label state bit #0997; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 0998", "Toggles the stat-review badge state bit #0998; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 0999", "Toggles the compatibility note state bit #0999; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1000", "Toggles the mini-mode tracker state bit #1000; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1001", "Toggles the spider-snap audit state bit #1001; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1002", "Toggles the trigger-pulse badge state bit #1002; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1003", "Toggles the pause-menu shortcut state bit #1003; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1004", "Toggles the color-channel memo state bit #1004; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1005", "Toggles the volume-pass reminder state bit #1005; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1006", "Toggles the profile-route marker state bit #1006; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1007", "Toggles the stress-test latch state bit #1007; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1008", "Toggles the player-speed notebook state bit #1008; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1009", "Toggles the swing-arc sampler state bit #1009; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1010", "Toggles the camera-frame ruler state bit #1010; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1011", "Toggles the timing-note bank state bit #1011; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1012", "Toggles the editor-layer bookmark state bit #1012; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1013", "Toggles the rhythm-window card state bit #1013; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1014", "Toggles the daily-run note state bit #1014; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1015", "Toggles the fallback preset state bit #1015; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1016", "Toggles the safe-practice reminder state bit #1016; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1017", "Toggles the release-safety latch state bit #1017; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1018", "Toggles the contrast-check overlay state bit #1018; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1019", "Toggles the route-bookmark cell state bit #1019; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1020", "Toggles the collision-block checklist state bit #1020; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1021", "Toggles the audio-trigger audit state bit #1021; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1022", "Toggles the reward-check tracker state bit #1022; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1023", "Toggles the integration flag state bit #1023; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1024", "Toggles the icon ghost guard state bit #1024; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1025", "Toggles the tap-window coach state bit #1025; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1026", "Toggles the hitbox-readability layer state bit #1026; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1027", "Toggles the checkpoint workflow state bit #1027; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1028", "Toggles the object-id scratchpad state bit #1028; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1029", "Toggles the beat-grid marker state bit #1029; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1030", "Toggles the achievement checklist state bit #1030; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1031", "Toggles the sandbox switch state bit #1031; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1032", "Toggles the gravity-state watcher state bit #1032; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1033", "Toggles the orb-route planner state bit #1033; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1034", "Toggles the background-noise switch state bit #1034; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1035", "Toggles the attempt-session marker state bit #1035; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1036", "Toggles the group-link inspector state bit #1036; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1037", "Toggles the SFX-channel note state bit #1037; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1038", "Toggles the account-sync reminder state bit #1038; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1039", "Toggles the prototype flag state bit #1039; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1040", "Toggles the dual-player lane marker state bit #1040; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1041", "Toggles the ship-stability hint state bit #1041; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1042", "Toggles the portal-color marker state bit #1042; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1043", "Toggles the speed-ladder slot state bit #1043; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1044", "Toggles the trigger-chain note state bit #1044; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1045", "Toggles the song-offset checker state bit #1045; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1046", "Toggles the progress-note slot state bit #1046; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1047", "Toggles the future-hook marker state bit #1047; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1048", "Toggles the damage-test latch state bit #1048; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1049", "Toggles the wave-lane monitor state bit #1049; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1050", "Toggles the slope-outline helper state bit #1050; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1051", "Toggles the restart preset state bit #1051; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1052", "Toggles the spawn-order label state bit #1052; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1053", "Toggles the metronome lane state bit #1053; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1054", "Toggles the quest-state card state bit #1054; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1055", "Toggles the diagnostic cell state bit #1055; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1056", "Toggles the spawn-state keeper state bit #1056; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1057", "Toggles the robot-hop timer state bit #1057; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1058", "Toggles the coin-path highlight state bit #1058; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1059", "Toggles the state-cleanup macro state bit #1059; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1060", "Toggles the move-action planner state bit #1060; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1061", "Toggles the sync-point label state bit #1061; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1062", "Toggles the stat-review badge state bit #1062; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1063", "Toggles the compatibility note state bit #1063; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1064", "Toggles the mini-mode tracker state bit #1064; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1065", "Toggles the spider-snap audit state bit #1065; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1066", "Toggles the trigger-pulse badge state bit #1066; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1067", "Toggles the pause-menu shortcut state bit #1067; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1068", "Toggles the color-channel memo state bit #1068; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1069", "Toggles the volume-pass reminder state bit #1069; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1070", "Toggles the profile-route marker state bit #1070; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1071", "Toggles the stress-test latch state bit #1071; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1072", "Toggles the player-speed notebook state bit #1072; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1073", "Toggles the swing-arc sampler state bit #1073; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1074", "Toggles the camera-frame ruler state bit #1074; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1075", "Toggles the timing-note bank state bit #1075; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1076", "Toggles the editor-layer bookmark state bit #1076; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1077", "Toggles the rhythm-window card state bit #1077; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1078", "Toggles the daily-run note state bit #1078; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1079", "Toggles the fallback preset state bit #1079; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1080", "Toggles the safe-practice reminder state bit #1080; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1081", "Toggles the release-safety latch state bit #1081; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1082", "Toggles the contrast-check overlay state bit #1082; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1083", "Toggles the route-bookmark cell state bit #1083; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1084", "Toggles the collision-block checklist state bit #1084; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1085", "Toggles the audio-trigger audit state bit #1085; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1086", "Toggles the reward-check tracker state bit #1086; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1087", "Toggles the integration flag state bit #1087; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1088", "Toggles the icon ghost guard state bit #1088; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1089", "Toggles the tap-window coach state bit #1089; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1090", "Toggles the hitbox-readability layer state bit #1090; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1091", "Toggles the checkpoint workflow state bit #1091; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1092", "Toggles the object-id scratchpad state bit #1092; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1093", "Toggles the beat-grid marker state bit #1093; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1094", "Toggles the achievement checklist state bit #1094; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1095", "Toggles the sandbox switch state bit #1095; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1096", "Toggles the gravity-state watcher state bit #1096; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1097", "Toggles the orb-route planner state bit #1097; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1098", "Toggles the background-noise switch state bit #1098; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1099", "Toggles the attempt-session marker state bit #1099; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1100", "Toggles the group-link inspector state bit #1100; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1101", "Toggles the SFX-channel note state bit #1101; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1102", "Toggles the account-sync reminder state bit #1102; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1103", "Toggles the prototype flag state bit #1103; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1104", "Toggles the dual-player lane marker state bit #1104; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1105", "Toggles the ship-stability hint state bit #1105; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1106", "Toggles the portal-color marker state bit #1106; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1107", "Toggles the speed-ladder slot state bit #1107; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1108", "Toggles the trigger-chain note state bit #1108; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1109", "Toggles the song-offset checker state bit #1109; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1110", "Toggles the progress-note slot state bit #1110; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1111", "Toggles the future-hook marker state bit #1111; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1112", "Toggles the damage-test latch state bit #1112; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1113", "Toggles the wave-lane monitor state bit #1113; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1114", "Toggles the slope-outline helper state bit #1114; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1115", "Toggles the restart preset state bit #1115; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1116", "Toggles the spawn-order label state bit #1116; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1117", "Toggles the metronome lane state bit #1117; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1118", "Toggles the quest-state card state bit #1118; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1119", "Toggles the diagnostic cell state bit #1119; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1120", "Toggles the spawn-state keeper state bit #1120; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1121", "Toggles the robot-hop timer state bit #1121; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1122", "Toggles the coin-path highlight state bit #1122; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1123", "Toggles the state-cleanup macro state bit #1123; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1124", "Toggles the move-action planner state bit #1124; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1125", "Toggles the sync-point label state bit #1125; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1126", "Toggles the stat-review badge state bit #1126; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1127", "Toggles the compatibility note state bit #1127; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1128", "Toggles the mini-mode tracker state bit #1128; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1129", "Toggles the spider-snap audit state bit #1129; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1130", "Toggles the trigger-pulse badge state bit #1130; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1131", "Toggles the pause-menu shortcut state bit #1131; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1132", "Toggles the color-channel memo state bit #1132; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1133", "Toggles the volume-pass reminder state bit #1133; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1134", "Toggles the profile-route marker state bit #1134; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1135", "Toggles the stress-test latch state bit #1135; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1136", "Toggles the player-speed notebook state bit #1136; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1137", "Toggles the swing-arc sampler state bit #1137; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1138", "Toggles the camera-frame ruler state bit #1138; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1139", "Toggles the timing-note bank state bit #1139; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1140", "Toggles the editor-layer bookmark state bit #1140; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1141", "Toggles the rhythm-window card state bit #1141; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1142", "Toggles the daily-run note state bit #1142; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1143", "Toggles the fallback preset state bit #1143; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1144", "Toggles the safe-practice reminder state bit #1144; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1145", "Toggles the release-safety latch state bit #1145; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1146", "Toggles the contrast-check overlay state bit #1146; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1147", "Toggles the route-bookmark cell state bit #1147; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1148", "Toggles the collision-block checklist state bit #1148; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1149", "Toggles the audio-trigger audit state bit #1149; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1150", "Toggles the reward-check tracker state bit #1150; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1151", "Toggles the integration flag state bit #1151; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1152", "Toggles the icon ghost guard state bit #1152; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1153", "Toggles the tap-window coach state bit #1153; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1154", "Toggles the hitbox-readability layer state bit #1154; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1155", "Toggles the checkpoint workflow state bit #1155; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1156", "Toggles the object-id scratchpad state bit #1156; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1157", "Toggles the beat-grid marker state bit #1157; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1158", "Toggles the achievement checklist state bit #1158; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1159", "Toggles the sandbox switch state bit #1159; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1160", "Toggles the gravity-state watcher state bit #1160; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1161", "Toggles the orb-route planner state bit #1161; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1162", "Toggles the background-noise switch state bit #1162; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1163", "Toggles the attempt-session marker state bit #1163; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1164", "Toggles the group-link inspector state bit #1164; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1165", "Toggles the SFX-channel note state bit #1165; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1166", "Toggles the account-sync reminder state bit #1166; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1167", "Toggles the prototype flag state bit #1167; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1168", "Toggles the dual-player lane marker state bit #1168; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1169", "Toggles the ship-stability hint state bit #1169; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1170", "Toggles the portal-color marker state bit #1170; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1171", "Toggles the speed-ladder slot state bit #1171; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1172", "Toggles the trigger-chain note state bit #1172; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1173", "Toggles the song-offset checker state bit #1173; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1174", "Toggles the progress-note slot state bit #1174; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1175", "Toggles the future-hook marker state bit #1175; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1176", "Toggles the damage-test latch state bit #1176; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1177", "Toggles the wave-lane monitor state bit #1177; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1178", "Toggles the slope-outline helper state bit #1178; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1179", "Toggles the restart preset state bit #1179; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1180", "Toggles the spawn-order label state bit #1180; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1181", "Toggles the metronome lane state bit #1181; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1182", "Toggles the quest-state card state bit #1182; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1183", "Toggles the diagnostic cell state bit #1183; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1184", "Toggles the spawn-state keeper state bit #1184; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1185", "Toggles the robot-hop timer state bit #1185; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1186", "Toggles the coin-path highlight state bit #1186; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1187", "Toggles the state-cleanup macro state bit #1187; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1188", "Toggles the move-action planner state bit #1188; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1189", "Toggles the sync-point label state bit #1189; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1190", "Toggles the stat-review badge state bit #1190; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1191", "Toggles the compatibility note state bit #1191; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1192", "Toggles the mini-mode tracker state bit #1192; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1193", "Toggles the spider-snap audit state bit #1193; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1194", "Toggles the trigger-pulse badge state bit #1194; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1195", "Toggles the pause-menu shortcut state bit #1195; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1196", "Toggles the color-channel memo state bit #1196; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1197", "Toggles the volume-pass reminder state bit #1197; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1198", "Toggles the profile-route marker state bit #1198; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1199", "Toggles the stress-test latch state bit #1199; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1200", "Toggles the player-speed notebook state bit #1200; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1201", "Toggles the swing-arc sampler state bit #1201; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1202", "Toggles the camera-frame ruler state bit #1202; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1203", "Toggles the timing-note bank state bit #1203; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1204", "Toggles the editor-layer bookmark state bit #1204; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1205", "Toggles the rhythm-window card state bit #1205; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1206", "Toggles the daily-run note state bit #1206; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1207", "Toggles the fallback preset state bit #1207; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1208", "Toggles the safe-practice reminder state bit #1208; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1209", "Toggles the release-safety latch state bit #1209; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1210", "Toggles the contrast-check overlay state bit #1210; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1211", "Toggles the route-bookmark cell state bit #1211; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1212", "Toggles the collision-block checklist state bit #1212; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1213", "Toggles the audio-trigger audit state bit #1213; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1214", "Toggles the reward-check tracker state bit #1214; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1215", "Toggles the integration flag state bit #1215; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1216", "Toggles the icon ghost guard state bit #1216; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1217", "Toggles the tap-window coach state bit #1217; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1218", "Toggles the hitbox-readability layer state bit #1218; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1219", "Toggles the checkpoint workflow state bit #1219; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1220", "Toggles the object-id scratchpad state bit #1220; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1221", "Toggles the beat-grid marker state bit #1221; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1222", "Toggles the achievement checklist state bit #1222; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1223", "Toggles the sandbox switch state bit #1223; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1224", "Toggles the gravity-state watcher state bit #1224; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1225", "Toggles the orb-route planner state bit #1225; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1226", "Toggles the background-noise switch state bit #1226; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1227", "Toggles the attempt-session marker state bit #1227; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1228", "Toggles the group-link inspector state bit #1228; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1229", "Toggles the SFX-channel note state bit #1229; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1230", "Toggles the account-sync reminder state bit #1230; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1231", "Toggles the prototype flag state bit #1231; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1232", "Toggles the dual-player lane marker state bit #1232; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1233", "Toggles the ship-stability hint state bit #1233; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1234", "Toggles the portal-color marker state bit #1234; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1235", "Toggles the speed-ladder slot state bit #1235; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1236", "Toggles the trigger-chain note state bit #1236; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1237", "Toggles the song-offset checker state bit #1237; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1238", "Toggles the progress-note slot state bit #1238; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1239", "Toggles the future-hook marker state bit #1239; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1240", "Toggles the damage-test latch state bit #1240; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1241", "Toggles the wave-lane monitor state bit #1241; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1242", "Toggles the slope-outline helper state bit #1242; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1243", "Toggles the restart preset state bit #1243; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1244", "Toggles the spawn-order label state bit #1244; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1245", "Toggles the metronome lane state bit #1245; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1246", "Toggles the quest-state card state bit #1246; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1247", "Toggles the diagnostic cell state bit #1247; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1248", "Toggles the spawn-state keeper state bit #1248; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1249", "Toggles the robot-hop timer state bit #1249; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1250", "Toggles the coin-path highlight state bit #1250; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1251", "Toggles the state-cleanup macro state bit #1251; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1252", "Toggles the move-action planner state bit #1252; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1253", "Toggles the sync-point label state bit #1253; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1254", "Toggles the stat-review badge state bit #1254; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1255", "Toggles the compatibility note state bit #1255; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1256", "Toggles the mini-mode tracker state bit #1256; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1257", "Toggles the spider-snap audit state bit #1257; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1258", "Toggles the trigger-pulse badge state bit #1258; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1259", "Toggles the pause-menu shortcut state bit #1259; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1260", "Toggles the color-channel memo state bit #1260; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1261", "Toggles the volume-pass reminder state bit #1261; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1262", "Toggles the profile-route marker state bit #1262; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1263", "Toggles the stress-test latch state bit #1263; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1264", "Toggles the player-speed notebook state bit #1264; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1265", "Toggles the swing-arc sampler state bit #1265; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1266", "Toggles the camera-frame ruler state bit #1266; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1267", "Toggles the timing-note bank state bit #1267; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1268", "Toggles the editor-layer bookmark state bit #1268; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1269", "Toggles the rhythm-window card state bit #1269; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1270", "Toggles the daily-run note state bit #1270; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1271", "Toggles the fallback preset state bit #1271; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1272", "Toggles the safe-practice reminder state bit #1272; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1273", "Toggles the release-safety latch state bit #1273; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1274", "Toggles the contrast-check overlay state bit #1274; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1275", "Toggles the route-bookmark cell state bit #1275; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1276", "Toggles the collision-block checklist state bit #1276; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1277", "Toggles the audio-trigger audit state bit #1277; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1278", "Toggles the reward-check tracker state bit #1278; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1279", "Toggles the integration flag state bit #1279; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1280", "Toggles the icon ghost guard state bit #1280; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1281", "Toggles the tap-window coach state bit #1281; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1282", "Toggles the hitbox-readability layer state bit #1282; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1283", "Toggles the checkpoint workflow state bit #1283; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1284", "Toggles the object-id scratchpad state bit #1284; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1285", "Toggles the beat-grid marker state bit #1285; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1286", "Toggles the achievement checklist state bit #1286; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1287", "Toggles the sandbox switch state bit #1287; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1288", "Toggles the gravity-state watcher state bit #1288; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1289", "Toggles the orb-route planner state bit #1289; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1290", "Toggles the background-noise switch state bit #1290; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1291", "Toggles the attempt-session marker state bit #1291; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1292", "Toggles the group-link inspector state bit #1292; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1293", "Toggles the SFX-channel note state bit #1293; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1294", "Toggles the account-sync reminder state bit #1294; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1295", "Toggles the prototype flag state bit #1295; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1296", "Toggles the dual-player lane marker state bit #1296; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1297", "Toggles the ship-stability hint state bit #1297; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1298", "Toggles the portal-color marker state bit #1298; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1299", "Toggles the speed-ladder slot state bit #1299; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1300", "Toggles the trigger-chain note state bit #1300; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1301", "Toggles the song-offset checker state bit #1301; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1302", "Toggles the progress-note slot state bit #1302; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1303", "Toggles the future-hook marker state bit #1303; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1304", "Toggles the damage-test latch state bit #1304; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1305", "Toggles the wave-lane monitor state bit #1305; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1306", "Toggles the slope-outline helper state bit #1306; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1307", "Toggles the restart preset state bit #1307; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1308", "Toggles the spawn-order label state bit #1308; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1309", "Toggles the metronome lane state bit #1309; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1310", "Toggles the quest-state card state bit #1310; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1311", "Toggles the diagnostic cell state bit #1311; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1312", "Toggles the spawn-state keeper state bit #1312; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1313", "Toggles the robot-hop timer state bit #1313; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1314", "Toggles the coin-path highlight state bit #1314; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1315", "Toggles the state-cleanup macro state bit #1315; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1316", "Toggles the move-action planner state bit #1316; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1317", "Toggles the sync-point label state bit #1317; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1318", "Toggles the stat-review badge state bit #1318; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1319", "Toggles the compatibility note state bit #1319; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1320", "Toggles the mini-mode tracker state bit #1320; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1321", "Toggles the spider-snap audit state bit #1321; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1322", "Toggles the trigger-pulse badge state bit #1322; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1323", "Toggles the pause-menu shortcut state bit #1323; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1324", "Toggles the color-channel memo state bit #1324; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1325", "Toggles the volume-pass reminder state bit #1325; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1326", "Toggles the profile-route marker state bit #1326; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1327", "Toggles the stress-test latch state bit #1327; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1328", "Toggles the player-speed notebook state bit #1328; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1329", "Toggles the swing-arc sampler state bit #1329; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1330", "Toggles the camera-frame ruler state bit #1330; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1331", "Toggles the timing-note bank state bit #1331; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1332", "Toggles the editor-layer bookmark state bit #1332; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1333", "Toggles the rhythm-window card state bit #1333; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1334", "Toggles the daily-run note state bit #1334; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1335", "Toggles the fallback preset state bit #1335; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1336", "Toggles the safe-practice reminder state bit #1336; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1337", "Toggles the release-safety latch state bit #1337; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1338", "Toggles the contrast-check overlay state bit #1338; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1339", "Toggles the route-bookmark cell state bit #1339; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1340", "Toggles the collision-block checklist state bit #1340; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1341", "Toggles the audio-trigger audit state bit #1341; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1342", "Toggles the reward-check tracker state bit #1342; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1343", "Toggles the integration flag state bit #1343; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1344", "Toggles the icon ghost guard state bit #1344; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1345", "Toggles the tap-window coach state bit #1345; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1346", "Toggles the hitbox-readability layer state bit #1346; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1347", "Toggles the checkpoint workflow state bit #1347; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1348", "Toggles the object-id scratchpad state bit #1348; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1349", "Toggles the beat-grid marker state bit #1349; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1350", "Toggles the achievement checklist state bit #1350; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1351", "Toggles the sandbox switch state bit #1351; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1352", "Toggles the gravity-state watcher state bit #1352; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1353", "Toggles the orb-route planner state bit #1353; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1354", "Toggles the background-noise switch state bit #1354; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1355", "Toggles the attempt-session marker state bit #1355; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1356", "Toggles the group-link inspector state bit #1356; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1357", "Toggles the SFX-channel note state bit #1357; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1358", "Toggles the account-sync reminder state bit #1358; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1359", "Toggles the prototype flag state bit #1359; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1360", "Toggles the dual-player lane marker state bit #1360; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1361", "Toggles the ship-stability hint state bit #1361; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1362", "Toggles the portal-color marker state bit #1362; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1363", "Toggles the speed-ladder slot state bit #1363; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1364", "Toggles the trigger-chain note state bit #1364; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1365", "Toggles the song-offset checker state bit #1365; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1366", "Toggles the progress-note slot state bit #1366; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1367", "Toggles the future-hook marker state bit #1367; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1368", "Toggles the damage-test latch state bit #1368; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1369", "Toggles the wave-lane monitor state bit #1369; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1370", "Toggles the slope-outline helper state bit #1370; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1371", "Toggles the restart preset state bit #1371; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1372", "Toggles the spawn-order label state bit #1372; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1373", "Toggles the metronome lane state bit #1373; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1374", "Toggles the quest-state card state bit #1374; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1375", "Toggles the diagnostic cell state bit #1375; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1376", "Toggles the spawn-state keeper state bit #1376; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1377", "Toggles the robot-hop timer state bit #1377; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1378", "Toggles the coin-path highlight state bit #1378; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1379", "Toggles the state-cleanup macro state bit #1379; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1380", "Toggles the move-action planner state bit #1380; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1381", "Toggles the sync-point label state bit #1381; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1382", "Toggles the stat-review badge state bit #1382; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1383", "Toggles the compatibility note state bit #1383; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1384", "Toggles the mini-mode tracker state bit #1384; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1385", "Toggles the spider-snap audit state bit #1385; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1386", "Toggles the trigger-pulse badge state bit #1386; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1387", "Toggles the pause-menu shortcut state bit #1387; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1388", "Toggles the color-channel memo state bit #1388; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1389", "Toggles the volume-pass reminder state bit #1389; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1390", "Toggles the profile-route marker state bit #1390; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1391", "Toggles the stress-test latch state bit #1391; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1392", "Toggles the player-speed notebook state bit #1392; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1393", "Toggles the swing-arc sampler state bit #1393; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1394", "Toggles the camera-frame ruler state bit #1394; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1395", "Toggles the timing-note bank state bit #1395; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1396", "Toggles the editor-layer bookmark state bit #1396; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1397", "Toggles the rhythm-window card state bit #1397; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1398", "Toggles the daily-run note state bit #1398; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1399", "Toggles the fallback preset state bit #1399; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1400", "Toggles the safe-practice reminder state bit #1400; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1401", "Toggles the release-safety latch state bit #1401; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1402", "Toggles the contrast-check overlay state bit #1402; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1403", "Toggles the route-bookmark cell state bit #1403; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1404", "Toggles the collision-block checklist state bit #1404; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1405", "Toggles the audio-trigger audit state bit #1405; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1406", "Toggles the reward-check tracker state bit #1406; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1407", "Toggles the integration flag state bit #1407; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1408", "Toggles the icon ghost guard state bit #1408; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1409", "Toggles the tap-window coach state bit #1409; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1410", "Toggles the hitbox-readability layer state bit #1410; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1411", "Toggles the checkpoint workflow state bit #1411; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1412", "Toggles the object-id scratchpad state bit #1412; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1413", "Toggles the beat-grid marker state bit #1413; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1414", "Toggles the achievement checklist state bit #1414; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1415", "Toggles the sandbox switch state bit #1415; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1416", "Toggles the gravity-state watcher state bit #1416; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1417", "Toggles the orb-route planner state bit #1417; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1418", "Toggles the background-noise switch state bit #1418; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1419", "Toggles the attempt-session marker state bit #1419; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1420", "Toggles the group-link inspector state bit #1420; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1421", "Toggles the SFX-channel note state bit #1421; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1422", "Toggles the account-sync reminder state bit #1422; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1423", "Toggles the prototype flag state bit #1423; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1424", "Toggles the dual-player lane marker state bit #1424; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1425", "Toggles the ship-stability hint state bit #1425; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1426", "Toggles the portal-color marker state bit #1426; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1427", "Toggles the speed-ladder slot state bit #1427; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1428", "Toggles the trigger-chain note state bit #1428; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1429", "Toggles the song-offset checker state bit #1429; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1430", "Toggles the progress-note slot state bit #1430; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1431", "Toggles the future-hook marker state bit #1431; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1432", "Toggles the damage-test latch state bit #1432; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1433", "Toggles the wave-lane monitor state bit #1433; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1434", "Toggles the slope-outline helper state bit #1434; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1435", "Toggles the restart preset state bit #1435; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1436", "Toggles the spawn-order label state bit #1436; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1437", "Toggles the metronome lane state bit #1437; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1438", "Toggles the quest-state card state bit #1438; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1439", "Toggles the diagnostic cell state bit #1439; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1440", "Toggles the spawn-state keeper state bit #1440; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1441", "Toggles the robot-hop timer state bit #1441; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1442", "Toggles the coin-path highlight state bit #1442; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1443", "Toggles the state-cleanup macro state bit #1443; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1444", "Toggles the move-action planner state bit #1444; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1445", "Toggles the sync-point label state bit #1445; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1446", "Toggles the stat-review badge state bit #1446; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1447", "Toggles the compatibility note state bit #1447; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1448", "Toggles the mini-mode tracker state bit #1448; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1449", "Toggles the spider-snap audit state bit #1449; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1450", "Toggles the trigger-pulse badge state bit #1450; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1451", "Toggles the pause-menu shortcut state bit #1451; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1452", "Toggles the color-channel memo state bit #1452; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1453", "Toggles the volume-pass reminder state bit #1453; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1454", "Toggles the profile-route marker state bit #1454; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1455", "Toggles the stress-test latch state bit #1455; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1456", "Toggles the player-speed notebook state bit #1456; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1457", "Toggles the swing-arc sampler state bit #1457; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1458", "Toggles the camera-frame ruler state bit #1458; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1459", "Toggles the timing-note bank state bit #1459; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1460", "Toggles the editor-layer bookmark state bit #1460; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1461", "Toggles the rhythm-window card state bit #1461; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1462", "Toggles the daily-run note state bit #1462; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1463", "Toggles the fallback preset state bit #1463; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1464", "Toggles the safe-practice reminder state bit #1464; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1465", "Toggles the release-safety latch state bit #1465; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1466", "Toggles the contrast-check overlay state bit #1466; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1467", "Toggles the route-bookmark cell state bit #1467; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1468", "Toggles the collision-block checklist state bit #1468; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1469", "Toggles the audio-trigger audit state bit #1469; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1470", "Toggles the reward-check tracker state bit #1470; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1471", "Toggles the integration flag state bit #1471; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1472", "Toggles the icon ghost guard state bit #1472; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1473", "Toggles the tap-window coach state bit #1473; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1474", "Toggles the hitbox-readability layer state bit #1474; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1475", "Toggles the checkpoint workflow state bit #1475; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1476", "Toggles the object-id scratchpad state bit #1476; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1477", "Toggles the beat-grid marker state bit #1477; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1478", "Toggles the achievement checklist state bit #1478; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1479", "Toggles the sandbox switch state bit #1479; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1480", "Toggles the gravity-state watcher state bit #1480; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1481", "Toggles the orb-route planner state bit #1481; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1482", "Toggles the background-noise switch state bit #1482; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1483", "Toggles the attempt-session marker state bit #1483; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1484", "Toggles the group-link inspector state bit #1484; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1485", "Toggles the SFX-channel note state bit #1485; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1486", "Toggles the account-sync reminder state bit #1486; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1487", "Toggles the prototype flag state bit #1487; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1488", "Toggles the dual-player lane marker state bit #1488; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1489", "Toggles the ship-stability hint state bit #1489; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1490", "Toggles the portal-color marker state bit #1490; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1491", "Toggles the speed-ladder slot state bit #1491; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1492", "Toggles the trigger-chain note state bit #1492; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1493", "Toggles the song-offset checker state bit #1493; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1494", "Toggles the progress-note slot state bit #1494; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1495", "Toggles the future-hook marker state bit #1495; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1496", "Toggles the damage-test latch state bit #1496; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1497", "Toggles the wave-lane monitor state bit #1497; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1498", "Toggles the slope-outline helper state bit #1498; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1499", "Toggles the restart preset state bit #1499; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1500", "Toggles the spawn-order label state bit #1500; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1501", "Toggles the metronome lane state bit #1501; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1502", "Toggles the quest-state card state bit #1502; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1503", "Toggles the diagnostic cell state bit #1503; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1504", "Toggles the spawn-state keeper state bit #1504; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1505", "Toggles the robot-hop timer state bit #1505; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1506", "Toggles the coin-path highlight state bit #1506; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1507", "Toggles the state-cleanup macro state bit #1507; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1508", "Toggles the move-action planner state bit #1508; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1509", "Toggles the sync-point label state bit #1509; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1510", "Toggles the stat-review badge state bit #1510; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1511", "Toggles the compatibility note state bit #1511; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1512", "Toggles the mini-mode tracker state bit #1512; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1513", "Toggles the spider-snap audit state bit #1513; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1514", "Toggles the trigger-pulse badge state bit #1514; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1515", "Toggles the pause-menu shortcut state bit #1515; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1516", "Toggles the color-channel memo state bit #1516; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1517", "Toggles the volume-pass reminder state bit #1517; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1518", "Toggles the profile-route marker state bit #1518; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1519", "Toggles the stress-test latch state bit #1519; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1520", "Toggles the player-speed notebook state bit #1520; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1521", "Toggles the swing-arc sampler state bit #1521; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1522", "Toggles the camera-frame ruler state bit #1522; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1523", "Toggles the timing-note bank state bit #1523; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1524", "Toggles the editor-layer bookmark state bit #1524; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1525", "Toggles the rhythm-window card state bit #1525; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1526", "Toggles the daily-run note state bit #1526; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1527", "Toggles the fallback preset state bit #1527; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1528", "Toggles the safe-practice reminder state bit #1528; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1529", "Toggles the release-safety latch state bit #1529; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1530", "Toggles the contrast-check overlay state bit #1530; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1531", "Toggles the route-bookmark cell state bit #1531; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1532", "Toggles the collision-block checklist state bit #1532; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1533", "Toggles the audio-trigger audit state bit #1533; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1534", "Toggles the reward-check tracker state bit #1534; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1535", "Toggles the integration flag state bit #1535; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1536", "Toggles the icon ghost guard state bit #1536; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1537", "Toggles the tap-window coach state bit #1537; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1538", "Toggles the hitbox-readability layer state bit #1538; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1539", "Toggles the checkpoint workflow state bit #1539; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1540", "Toggles the object-id scratchpad state bit #1540; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1541", "Toggles the beat-grid marker state bit #1541; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1542", "Toggles the achievement checklist state bit #1542; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1543", "Toggles the sandbox switch state bit #1543; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1544", "Toggles the gravity-state watcher state bit #1544; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1545", "Toggles the orb-route planner state bit #1545; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1546", "Toggles the background-noise switch state bit #1546; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1547", "Toggles the attempt-session marker state bit #1547; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1548", "Toggles the group-link inspector state bit #1548; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1549", "Toggles the SFX-channel note state bit #1549; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1550", "Toggles the account-sync reminder state bit #1550; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1551", "Toggles the prototype flag state bit #1551; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1552", "Toggles the dual-player lane marker state bit #1552; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1553", "Toggles the ship-stability hint state bit #1553; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1554", "Toggles the portal-color marker state bit #1554; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1555", "Toggles the speed-ladder slot state bit #1555; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1556", "Toggles the trigger-chain note state bit #1556; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1557", "Toggles the song-offset checker state bit #1557; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1558", "Toggles the progress-note slot state bit #1558; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1559", "Toggles the future-hook marker state bit #1559; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1560", "Toggles the damage-test latch state bit #1560; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1561", "Toggles the wave-lane monitor state bit #1561; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1562", "Toggles the slope-outline helper state bit #1562; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1563", "Toggles the restart preset state bit #1563; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1564", "Toggles the spawn-order label state bit #1564; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1565", "Toggles the metronome lane state bit #1565; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1566", "Toggles the quest-state card state bit #1566; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1567", "Toggles the diagnostic cell state bit #1567; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1568", "Toggles the spawn-state keeper state bit #1568; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1569", "Toggles the robot-hop timer state bit #1569; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1570", "Toggles the coin-path highlight state bit #1570; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1571", "Toggles the state-cleanup macro state bit #1571; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1572", "Toggles the move-action planner state bit #1572; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1573", "Toggles the sync-point label state bit #1573; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1574", "Toggles the stat-review badge state bit #1574; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1575", "Toggles the compatibility note state bit #1575; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1576", "Toggles the mini-mode tracker state bit #1576; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1577", "Toggles the spider-snap audit state bit #1577; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1578", "Toggles the trigger-pulse badge state bit #1578; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1579", "Toggles the pause-menu shortcut state bit #1579; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1580", "Toggles the color-channel memo state bit #1580; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1581", "Toggles the volume-pass reminder state bit #1581; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1582", "Toggles the profile-route marker state bit #1582; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1583", "Toggles the stress-test latch state bit #1583; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1584", "Toggles the player-speed notebook state bit #1584; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1585", "Toggles the swing-arc sampler state bit #1585; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1586", "Toggles the camera-frame ruler state bit #1586; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1587", "Toggles the timing-note bank state bit #1587; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1588", "Toggles the editor-layer bookmark state bit #1588; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1589", "Toggles the rhythm-window card state bit #1589; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1590", "Toggles the daily-run note state bit #1590; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1591", "Toggles the fallback preset state bit #1591; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1592", "Toggles the safe-practice reminder state bit #1592; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1593", "Toggles the release-safety latch state bit #1593; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1594", "Toggles the contrast-check overlay state bit #1594; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1595", "Toggles the route-bookmark cell state bit #1595; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1596", "Toggles the collision-block checklist state bit #1596; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1597", "Toggles the audio-trigger audit state bit #1597; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1598", "Toggles the reward-check tracker state bit #1598; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1599", "Toggles the integration flag state bit #1599; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1600", "Toggles the icon ghost guard state bit #1600; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1601", "Toggles the tap-window coach state bit #1601; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1602", "Toggles the hitbox-readability layer state bit #1602; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1603", "Toggles the checkpoint workflow state bit #1603; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1604", "Toggles the object-id scratchpad state bit #1604; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1605", "Toggles the beat-grid marker state bit #1605; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1606", "Toggles the achievement checklist state bit #1606; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1607", "Toggles the sandbox switch state bit #1607; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1608", "Toggles the gravity-state watcher state bit #1608; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1609", "Toggles the orb-route planner state bit #1609; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1610", "Toggles the background-noise switch state bit #1610; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1611", "Toggles the attempt-session marker state bit #1611; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1612", "Toggles the group-link inspector state bit #1612; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1613", "Toggles the SFX-channel note state bit #1613; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1614", "Toggles the account-sync reminder state bit #1614; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1615", "Toggles the prototype flag state bit #1615; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1616", "Toggles the dual-player lane marker state bit #1616; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1617", "Toggles the ship-stability hint state bit #1617; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1618", "Toggles the portal-color marker state bit #1618; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1619", "Toggles the speed-ladder slot state bit #1619; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1620", "Toggles the trigger-chain note state bit #1620; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1621", "Toggles the song-offset checker state bit #1621; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1622", "Toggles the progress-note slot state bit #1622; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1623", "Toggles the future-hook marker state bit #1623; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1624", "Toggles the damage-test latch state bit #1624; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1625", "Toggles the wave-lane monitor state bit #1625; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1626", "Toggles the slope-outline helper state bit #1626; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1627", "Toggles the restart preset state bit #1627; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1628", "Toggles the spawn-order label state bit #1628; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1629", "Toggles the metronome lane state bit #1629; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1630", "Toggles the quest-state card state bit #1630; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1631", "Toggles the diagnostic cell state bit #1631; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1632", "Toggles the spawn-state keeper state bit #1632; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1633", "Toggles the robot-hop timer state bit #1633; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1634", "Toggles the coin-path highlight state bit #1634; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1635", "Toggles the state-cleanup macro state bit #1635; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1636", "Toggles the move-action planner state bit #1636; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1637", "Toggles the sync-point label state bit #1637; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1638", "Toggles the stat-review badge state bit #1638; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1639", "Toggles the compatibility note state bit #1639; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1640", "Toggles the mini-mode tracker state bit #1640; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1641", "Toggles the spider-snap audit state bit #1641; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1642", "Toggles the trigger-pulse badge state bit #1642; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1643", "Toggles the pause-menu shortcut state bit #1643; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1644", "Toggles the color-channel memo state bit #1644; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1645", "Toggles the volume-pass reminder state bit #1645; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1646", "Toggles the profile-route marker state bit #1646; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1647", "Toggles the stress-test latch state bit #1647; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1648", "Toggles the player-speed notebook state bit #1648; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1649", "Toggles the swing-arc sampler state bit #1649; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1650", "Toggles the camera-frame ruler state bit #1650; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1651", "Toggles the timing-note bank state bit #1651; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1652", "Toggles the editor-layer bookmark state bit #1652; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1653", "Toggles the rhythm-window card state bit #1653; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1654", "Toggles the daily-run note state bit #1654; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1655", "Toggles the fallback preset state bit #1655; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1656", "Toggles the safe-practice reminder state bit #1656; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1657", "Toggles the release-safety latch state bit #1657; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1658", "Toggles the contrast-check overlay state bit #1658; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1659", "Toggles the route-bookmark cell state bit #1659; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1660", "Toggles the collision-block checklist state bit #1660; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1661", "Toggles the audio-trigger audit state bit #1661; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1662", "Toggles the reward-check tracker state bit #1662; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1663", "Toggles the integration flag state bit #1663; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1664", "Toggles the icon ghost guard state bit #1664; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1665", "Toggles the tap-window coach state bit #1665; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1666", "Toggles the hitbox-readability layer state bit #1666; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1667", "Toggles the checkpoint workflow state bit #1667; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1668", "Toggles the object-id scratchpad state bit #1668; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1669", "Toggles the beat-grid marker state bit #1669; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1670", "Toggles the achievement checklist state bit #1670; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1671", "Toggles the sandbox switch state bit #1671; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1672", "Toggles the gravity-state watcher state bit #1672; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1673", "Toggles the orb-route planner state bit #1673; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1674", "Toggles the background-noise switch state bit #1674; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1675", "Toggles the attempt-session marker state bit #1675; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1676", "Toggles the group-link inspector state bit #1676; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1677", "Toggles the SFX-channel note state bit #1677; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1678", "Toggles the account-sync reminder state bit #1678; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1679", "Toggles the prototype flag state bit #1679; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1680", "Toggles the dual-player lane marker state bit #1680; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1681", "Toggles the ship-stability hint state bit #1681; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1682", "Toggles the portal-color marker state bit #1682; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1683", "Toggles the speed-ladder slot state bit #1683; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1684", "Toggles the trigger-chain note state bit #1684; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1685", "Toggles the song-offset checker state bit #1685; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1686", "Toggles the progress-note slot state bit #1686; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1687", "Toggles the future-hook marker state bit #1687; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1688", "Toggles the damage-test latch state bit #1688; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1689", "Toggles the wave-lane monitor state bit #1689; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1690", "Toggles the slope-outline helper state bit #1690; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1691", "Toggles the restart preset state bit #1691; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1692", "Toggles the spawn-order label state bit #1692; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1693", "Toggles the metronome lane state bit #1693; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1694", "Toggles the quest-state card state bit #1694; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1695", "Toggles the diagnostic cell state bit #1695; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1696", "Toggles the spawn-state keeper state bit #1696; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1697", "Toggles the robot-hop timer state bit #1697; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1698", "Toggles the coin-path highlight state bit #1698; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1699", "Toggles the state-cleanup macro state bit #1699; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1700", "Toggles the move-action planner state bit #1700; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1701", "Toggles the sync-point label state bit #1701; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1702", "Toggles the stat-review badge state bit #1702; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1703", "Toggles the compatibility note state bit #1703; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1704", "Toggles the mini-mode tracker state bit #1704; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1705", "Toggles the spider-snap audit state bit #1705; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1706", "Toggles the trigger-pulse badge state bit #1706; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1707", "Toggles the pause-menu shortcut state bit #1707; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1708", "Toggles the color-channel memo state bit #1708; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1709", "Toggles the volume-pass reminder state bit #1709; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1710", "Toggles the profile-route marker state bit #1710; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1711", "Toggles the stress-test latch state bit #1711; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1712", "Toggles the player-speed notebook state bit #1712; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1713", "Toggles the swing-arc sampler state bit #1713; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1714", "Toggles the camera-frame ruler state bit #1714; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1715", "Toggles the timing-note bank state bit #1715; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1716", "Toggles the editor-layer bookmark state bit #1716; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1717", "Toggles the rhythm-window card state bit #1717; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1718", "Toggles the daily-run note state bit #1718; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1719", "Toggles the fallback preset state bit #1719; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1720", "Toggles the safe-practice reminder state bit #1720; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1721", "Toggles the release-safety latch state bit #1721; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1722", "Toggles the contrast-check overlay state bit #1722; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1723", "Toggles the route-bookmark cell state bit #1723; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1724", "Toggles the collision-block checklist state bit #1724; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1725", "Toggles the audio-trigger audit state bit #1725; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1726", "Toggles the reward-check tracker state bit #1726; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1727", "Toggles the integration flag state bit #1727; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1728", "Toggles the icon ghost guard state bit #1728; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1729", "Toggles the tap-window coach state bit #1729; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1730", "Toggles the hitbox-readability layer state bit #1730; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1731", "Toggles the checkpoint workflow state bit #1731; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1732", "Toggles the object-id scratchpad state bit #1732; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1733", "Toggles the beat-grid marker state bit #1733; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1734", "Toggles the achievement checklist state bit #1734; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1735", "Toggles the sandbox switch state bit #1735; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1736", "Toggles the gravity-state watcher state bit #1736; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1737", "Toggles the orb-route planner state bit #1737; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1738", "Toggles the background-noise switch state bit #1738; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1739", "Toggles the attempt-session marker state bit #1739; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1740", "Toggles the group-link inspector state bit #1740; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1741", "Toggles the SFX-channel note state bit #1741; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1742", "Toggles the account-sync reminder state bit #1742; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1743", "Toggles the prototype flag state bit #1743; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1744", "Toggles the dual-player lane marker state bit #1744; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1745", "Toggles the ship-stability hint state bit #1745; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1746", "Toggles the portal-color marker state bit #1746; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1747", "Toggles the speed-ladder slot state bit #1747; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1748", "Toggles the trigger-chain note state bit #1748; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1749", "Toggles the song-offset checker state bit #1749; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1750", "Toggles the progress-note slot state bit #1750; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1751", "Toggles the future-hook marker state bit #1751; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1752", "Toggles the damage-test latch state bit #1752; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1753", "Toggles the wave-lane monitor state bit #1753; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1754", "Toggles the slope-outline helper state bit #1754; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1755", "Toggles the restart preset state bit #1755; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1756", "Toggles the spawn-order label state bit #1756; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1757", "Toggles the metronome lane state bit #1757; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1758", "Toggles the quest-state card state bit #1758; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1759", "Toggles the diagnostic cell state bit #1759; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1760", "Toggles the spawn-state keeper state bit #1760; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1761", "Toggles the robot-hop timer state bit #1761; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1762", "Toggles the coin-path highlight state bit #1762; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1763", "Toggles the state-cleanup macro state bit #1763; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1764", "Toggles the move-action planner state bit #1764; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1765", "Toggles the sync-point label state bit #1765; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1766", "Toggles the stat-review badge state bit #1766; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1767", "Toggles the compatibility note state bit #1767; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1768", "Toggles the mini-mode tracker state bit #1768; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1769", "Toggles the spider-snap audit state bit #1769; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1770", "Toggles the trigger-pulse badge state bit #1770; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1771", "Toggles the pause-menu shortcut state bit #1771; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1772", "Toggles the color-channel memo state bit #1772; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1773", "Toggles the volume-pass reminder state bit #1773; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1774", "Toggles the profile-route marker state bit #1774; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1775", "Toggles the stress-test latch state bit #1775; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1776", "Toggles the player-speed notebook state bit #1776; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1777", "Toggles the swing-arc sampler state bit #1777; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1778", "Toggles the camera-frame ruler state bit #1778; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1779", "Toggles the timing-note bank state bit #1779; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1780", "Toggles the editor-layer bookmark state bit #1780; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1781", "Toggles the rhythm-window card state bit #1781; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1782", "Toggles the daily-run note state bit #1782; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1783", "Toggles the fallback preset state bit #1783; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1784", "Toggles the safe-practice reminder state bit #1784; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1785", "Toggles the release-safety latch state bit #1785; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1786", "Toggles the contrast-check overlay state bit #1786; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1787", "Toggles the route-bookmark cell state bit #1787; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1788", "Toggles the collision-block checklist state bit #1788; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1789", "Toggles the audio-trigger audit state bit #1789; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1790", "Toggles the reward-check tracker state bit #1790; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1791", "Toggles the integration flag state bit #1791; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1792", "Toggles the icon ghost guard state bit #1792; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1793", "Toggles the tap-window coach state bit #1793; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1794", "Toggles the hitbox-readability layer state bit #1794; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1795", "Toggles the checkpoint workflow state bit #1795; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1796", "Toggles the object-id scratchpad state bit #1796; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1797", "Toggles the beat-grid marker state bit #1797; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1798", "Toggles the achievement checklist state bit #1798; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1799", "Toggles the sandbox switch state bit #1799; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1800", "Toggles the gravity-state watcher state bit #1800; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1801", "Toggles the orb-route planner state bit #1801; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1802", "Toggles the background-noise switch state bit #1802; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1803", "Toggles the attempt-session marker state bit #1803; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1804", "Toggles the group-link inspector state bit #1804; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1805", "Toggles the SFX-channel note state bit #1805; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1806", "Toggles the account-sync reminder state bit #1806; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1807", "Toggles the prototype flag state bit #1807; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1808", "Toggles the dual-player lane marker state bit #1808; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1809", "Toggles the ship-stability hint state bit #1809; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1810", "Toggles the portal-color marker state bit #1810; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1811", "Toggles the speed-ladder slot state bit #1811; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1812", "Toggles the trigger-chain note state bit #1812; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1813", "Toggles the song-offset checker state bit #1813; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1814", "Toggles the progress-note slot state bit #1814; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1815", "Toggles the future-hook marker state bit #1815; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1816", "Toggles the damage-test latch state bit #1816; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1817", "Toggles the wave-lane monitor state bit #1817; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1818", "Toggles the slope-outline helper state bit #1818; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1819", "Toggles the restart preset state bit #1819; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1820", "Toggles the spawn-order label state bit #1820; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1821", "Toggles the metronome lane state bit #1821; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1822", "Toggles the quest-state card state bit #1822; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1823", "Toggles the diagnostic cell state bit #1823; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1824", "Toggles the spawn-state keeper state bit #1824; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1825", "Toggles the robot-hop timer state bit #1825; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1826", "Toggles the coin-path highlight state bit #1826; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1827", "Toggles the state-cleanup macro state bit #1827; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1828", "Toggles the move-action planner state bit #1828; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1829", "Toggles the sync-point label state bit #1829; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1830", "Toggles the stat-review badge state bit #1830; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1831", "Toggles the compatibility note state bit #1831; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1832", "Toggles the mini-mode tracker state bit #1832; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1833", "Toggles the spider-snap audit state bit #1833; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1834", "Toggles the trigger-pulse badge state bit #1834; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1835", "Toggles the pause-menu shortcut state bit #1835; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1836", "Toggles the color-channel memo state bit #1836; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1837", "Toggles the volume-pass reminder state bit #1837; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1838", "Toggles the profile-route marker state bit #1838; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1839", "Toggles the stress-test latch state bit #1839; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1840", "Toggles the player-speed notebook state bit #1840; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1841", "Toggles the swing-arc sampler state bit #1841; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1842", "Toggles the camera-frame ruler state bit #1842; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1843", "Toggles the timing-note bank state bit #1843; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1844", "Toggles the editor-layer bookmark state bit #1844; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1845", "Toggles the rhythm-window card state bit #1845; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1846", "Toggles the daily-run note state bit #1846; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1847", "Toggles the fallback preset state bit #1847; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1848", "Toggles the safe-practice reminder state bit #1848; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1849", "Toggles the release-safety latch state bit #1849; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1850", "Toggles the contrast-check overlay state bit #1850; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1851", "Toggles the route-bookmark cell state bit #1851; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1852", "Toggles the collision-block checklist state bit #1852; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1853", "Toggles the audio-trigger audit state bit #1853; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1854", "Toggles the reward-check tracker state bit #1854; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1855", "Toggles the integration flag state bit #1855; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1856", "Toggles the icon ghost guard state bit #1856; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1857", "Toggles the tap-window coach state bit #1857; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1858", "Toggles the hitbox-readability layer state bit #1858; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1859", "Toggles the checkpoint workflow state bit #1859; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1860", "Toggles the object-id scratchpad state bit #1860; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1861", "Toggles the beat-grid marker state bit #1861; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1862", "Toggles the achievement checklist state bit #1862; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1863", "Toggles the sandbox switch state bit #1863; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1864", "Toggles the gravity-state watcher state bit #1864; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1865", "Toggles the orb-route planner state bit #1865; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1866", "Toggles the background-noise switch state bit #1866; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1867", "Toggles the attempt-session marker state bit #1867; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1868", "Toggles the group-link inspector state bit #1868; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1869", "Toggles the SFX-channel note state bit #1869; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1870", "Toggles the account-sync reminder state bit #1870; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1871", "Toggles the prototype flag state bit #1871; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1872", "Toggles the dual-player lane marker state bit #1872; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1873", "Toggles the ship-stability hint state bit #1873; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1874", "Toggles the portal-color marker state bit #1874; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1875", "Toggles the speed-ladder slot state bit #1875; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1876", "Toggles the trigger-chain note state bit #1876; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1877", "Toggles the song-offset checker state bit #1877; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1878", "Toggles the progress-note slot state bit #1878; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1879", "Toggles the future-hook marker state bit #1879; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1880", "Toggles the damage-test latch state bit #1880; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1881", "Toggles the wave-lane monitor state bit #1881; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1882", "Toggles the slope-outline helper state bit #1882; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1883", "Toggles the restart preset state bit #1883; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1884", "Toggles the spawn-order label state bit #1884; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1885", "Toggles the metronome lane state bit #1885; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1886", "Toggles the quest-state card state bit #1886; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1887", "Toggles the diagnostic cell state bit #1887; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1888", "Toggles the spawn-state keeper state bit #1888; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1889", "Toggles the robot-hop timer state bit #1889; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1890", "Toggles the coin-path highlight state bit #1890; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1891", "Toggles the state-cleanup macro state bit #1891; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1892", "Toggles the move-action planner state bit #1892; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1893", "Toggles the sync-point label state bit #1893; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1894", "Toggles the stat-review badge state bit #1894; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1895", "Toggles the compatibility note state bit #1895; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1896", "Toggles the mini-mode tracker state bit #1896; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1897", "Toggles the spider-snap audit state bit #1897; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1898", "Toggles the trigger-pulse badge state bit #1898; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1899", "Toggles the pause-menu shortcut state bit #1899; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1900", "Toggles the color-channel memo state bit #1900; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1901", "Toggles the volume-pass reminder state bit #1901; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1902", "Toggles the profile-route marker state bit #1902; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1903", "Toggles the stress-test latch state bit #1903; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1904", "Toggles the player-speed notebook state bit #1904; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1905", "Toggles the swing-arc sampler state bit #1905; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1906", "Toggles the camera-frame ruler state bit #1906; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1907", "Toggles the timing-note bank state bit #1907; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1908", "Toggles the editor-layer bookmark state bit #1908; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1909", "Toggles the rhythm-window card state bit #1909; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1910", "Toggles the daily-run note state bit #1910; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1911", "Toggles the fallback preset state bit #1911; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1912", "Toggles the safe-practice reminder state bit #1912; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1913", "Toggles the release-safety latch state bit #1913; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1914", "Toggles the contrast-check overlay state bit #1914; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1915", "Toggles the route-bookmark cell state bit #1915; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1916", "Toggles the collision-block checklist state bit #1916; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1917", "Toggles the audio-trigger audit state bit #1917; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1918", "Toggles the reward-check tracker state bit #1918; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1919", "Toggles the integration flag state bit #1919; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1920", "Toggles the icon ghost guard state bit #1920; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1921", "Toggles the tap-window coach state bit #1921; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1922", "Toggles the hitbox-readability layer state bit #1922; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1923", "Toggles the checkpoint workflow state bit #1923; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1924", "Toggles the object-id scratchpad state bit #1924; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1925", "Toggles the beat-grid marker state bit #1925; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1926", "Toggles the achievement checklist state bit #1926; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1927", "Toggles the sandbox switch state bit #1927; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1928", "Toggles the gravity-state watcher state bit #1928; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1929", "Toggles the orb-route planner state bit #1929; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1930", "Toggles the background-noise switch state bit #1930; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1931", "Toggles the attempt-session marker state bit #1931; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1932", "Toggles the group-link inspector state bit #1932; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1933", "Toggles the SFX-channel note state bit #1933; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1934", "Toggles the account-sync reminder state bit #1934; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1935", "Toggles the prototype flag state bit #1935; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 1936", "Toggles the dual-player lane marker state bit #1936; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 1937", "Toggles the ship-stability hint state bit #1937; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 1938", "Toggles the portal-color marker state bit #1938; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 1939", "Toggles the speed-ladder slot state bit #1939; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 1940", "Toggles the trigger-chain note state bit #1940; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 1941", "Toggles the song-offset checker state bit #1941; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 1942", "Toggles the progress-note slot state bit #1942; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 1943", "Toggles the future-hook marker state bit #1943; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 1944", "Toggles the damage-test latch state bit #1944; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 1945", "Toggles the wave-lane monitor state bit #1945; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 1946", "Toggles the slope-outline helper state bit #1946; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 1947", "Toggles the restart preset state bit #1947; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 1948", "Toggles the spawn-order label state bit #1948; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 1949", "Toggles the metronome lane state bit #1949; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 1950", "Toggles the quest-state card state bit #1950; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 1951", "Toggles the diagnostic cell state bit #1951; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 1952", "Toggles the spawn-state keeper state bit #1952; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 1953", "Toggles the robot-hop timer state bit #1953; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 1954", "Toggles the coin-path highlight state bit #1954; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 1955", "Toggles the state-cleanup macro state bit #1955; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 1956", "Toggles the move-action planner state bit #1956; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 1957", "Toggles the sync-point label state bit #1957; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 1958", "Toggles the stat-review badge state bit #1958; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 1959", "Toggles the compatibility note state bit #1959; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 1960", "Toggles the mini-mode tracker state bit #1960; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 1961", "Toggles the spider-snap audit state bit #1961; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 1962", "Toggles the trigger-pulse badge state bit #1962; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 1963", "Toggles the pause-menu shortcut state bit #1963; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 1964", "Toggles the color-channel memo state bit #1964; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 1965", "Toggles the volume-pass reminder state bit #1965; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 1966", "Toggles the profile-route marker state bit #1966; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 1967", "Toggles the stress-test latch state bit #1967; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 1968", "Toggles the player-speed notebook state bit #1968; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 1969", "Toggles the swing-arc sampler state bit #1969; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 1970", "Toggles the camera-frame ruler state bit #1970; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 1971", "Toggles the timing-note bank state bit #1971; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 1972", "Toggles the editor-layer bookmark state bit #1972; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 1973", "Toggles the rhythm-window card state bit #1973; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 1974", "Toggles the daily-run note state bit #1974; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 1975", "Toggles the fallback preset state bit #1975; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 1976", "Toggles the safe-practice reminder state bit #1976; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 1977", "Toggles the release-safety latch state bit #1977; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 1978", "Toggles the contrast-check overlay state bit #1978; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 1979", "Toggles the route-bookmark cell state bit #1979; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 1980", "Toggles the collision-block checklist state bit #1980; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 1981", "Toggles the audio-trigger audit state bit #1981; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 1982", "Toggles the reward-check tracker state bit #1982; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 1983", "Toggles the integration flag state bit #1983; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 1984", "Toggles the icon ghost guard state bit #1984; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 1985", "Toggles the tap-window coach state bit #1985; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 1986", "Toggles the hitbox-readability layer state bit #1986; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 1987", "Toggles the checkpoint workflow state bit #1987; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 1988", "Toggles the object-id scratchpad state bit #1988; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 1989", "Toggles the beat-grid marker state bit #1989; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 1990", "Toggles the achievement checklist state bit #1990; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 1991", "Toggles the sandbox switch state bit #1991; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 1992", "Toggles the gravity-state watcher state bit #1992; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 1993", "Toggles the orb-route planner state bit #1993; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 1994", "Toggles the background-noise switch state bit #1994; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 1995", "Toggles the attempt-session marker state bit #1995; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 1996", "Toggles the group-link inspector state bit #1996; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 1997", "Toggles the SFX-channel note state bit #1997; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 1998", "Toggles the account-sync reminder state bit #1998; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 1999", "Toggles the prototype flag state bit #1999; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 2000", "Toggles the dual-player lane marker state bit #2000; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 2001", "Toggles the ship-stability hint state bit #2001; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 2002", "Toggles the portal-color marker state bit #2002; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 2003", "Toggles the speed-ladder slot state bit #2003; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 2004", "Toggles the trigger-chain note state bit #2004; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 2005", "Toggles the song-offset checker state bit #2005; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 2006", "Toggles the progress-note slot state bit #2006; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 2007", "Toggles the future-hook marker state bit #2007; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 2008", "Toggles the damage-test latch state bit #2008; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 2009", "Toggles the wave-lane monitor state bit #2009; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 2010", "Toggles the slope-outline helper state bit #2010; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 2011", "Toggles the restart preset state bit #2011; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 2012", "Toggles the spawn-order label state bit #2012; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 2013", "Toggles the metronome lane state bit #2013; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 2014", "Toggles the quest-state card state bit #2014; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 2015", "Toggles the diagnostic cell state bit #2015; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 2016", "Toggles the spawn-state keeper state bit #2016; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 2017", "Toggles the robot-hop timer state bit #2017; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 2018", "Toggles the coin-path highlight state bit #2018; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 2019", "Toggles the state-cleanup macro state bit #2019; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 2020", "Toggles the move-action planner state bit #2020; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 2021", "Toggles the sync-point label state bit #2021; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 2022", "Toggles the stat-review badge state bit #2022; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 2023", "Toggles the compatibility note state bit #2023; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 2024", "Toggles the mini-mode tracker state bit #2024; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 2025", "Toggles the spider-snap audit state bit #2025; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 2026", "Toggles the trigger-pulse badge state bit #2026; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 2027", "Toggles the pause-menu shortcut state bit #2027; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 2028", "Toggles the color-channel memo state bit #2028; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 2029", "Toggles the volume-pass reminder state bit #2029; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 2030", "Toggles the profile-route marker state bit #2030; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 2031", "Toggles the stress-test latch state bit #2031; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 2032", "Toggles the player-speed notebook state bit #2032; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 2033", "Toggles the swing-arc sampler state bit #2033; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 2034", "Toggles the camera-frame ruler state bit #2034; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 2035", "Toggles the timing-note bank state bit #2035; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 2036", "Toggles the editor-layer bookmark state bit #2036; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 2037", "Toggles the rhythm-window card state bit #2037; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 2038", "Toggles the daily-run note state bit #2038; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 2039", "Toggles the fallback preset state bit #2039; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 2040", "Toggles the safe-practice reminder state bit #2040; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 2041", "Toggles the release-safety latch state bit #2041; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 2042", "Toggles the contrast-check overlay state bit #2042; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 2043", "Toggles the route-bookmark cell state bit #2043; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 2044", "Toggles the collision-block checklist state bit #2044; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 2045", "Toggles the audio-trigger audit state bit #2045; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 2046", "Toggles the reward-check tracker state bit #2046; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 2047", "Toggles the integration flag state bit #2047; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 2048", "Toggles the icon ghost guard state bit #2048; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 2049", "Toggles the tap-window coach state bit #2049; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 2050", "Toggles the hitbox-readability layer state bit #2050; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 2051", "Toggles the checkpoint workflow state bit #2051; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 2052", "Toggles the object-id scratchpad state bit #2052; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 2053", "Toggles the beat-grid marker state bit #2053; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 2054", "Toggles the achievement checklist state bit #2054; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 2055", "Toggles the sandbox switch state bit #2055; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 2056", "Toggles the gravity-state watcher state bit #2056; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 2057", "Toggles the orb-route planner state bit #2057; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 2058", "Toggles the background-noise switch state bit #2058; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 2059", "Toggles the attempt-session marker state bit #2059; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 2060", "Toggles the group-link inspector state bit #2060; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 2061", "Toggles the SFX-channel note state bit #2061; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 2062", "Toggles the account-sync reminder state bit #2062; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 2063", "Toggles the prototype flag state bit #2063; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 2064", "Toggles the dual-player lane marker state bit #2064; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 2065", "Toggles the ship-stability hint state bit #2065; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 2066", "Toggles the portal-color marker state bit #2066; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 2067", "Toggles the speed-ladder slot state bit #2067; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 2068", "Toggles the trigger-chain note state bit #2068; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 2069", "Toggles the song-offset checker state bit #2069; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 2070", "Toggles the progress-note slot state bit #2070; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 2071", "Toggles the future-hook marker state bit #2071; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 2072", "Toggles the damage-test latch state bit #2072; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 2073", "Toggles the wave-lane monitor state bit #2073; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 2074", "Toggles the slope-outline helper state bit #2074; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 2075", "Toggles the restart preset state bit #2075; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 2076", "Toggles the spawn-order label state bit #2076; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 2077", "Toggles the metronome lane state bit #2077; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 2078", "Toggles the quest-state card state bit #2078; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 2079", "Toggles the diagnostic cell state bit #2079; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 2080", "Toggles the spawn-state keeper state bit #2080; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 2081", "Toggles the robot-hop timer state bit #2081; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 2082", "Toggles the coin-path highlight state bit #2082; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 2083", "Toggles the state-cleanup macro state bit #2083; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 2084", "Toggles the move-action planner state bit #2084; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 2085", "Toggles the sync-point label state bit #2085; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 2086", "Toggles the stat-review badge state bit #2086; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 2087", "Toggles the compatibility note state bit #2087; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 2088", "Toggles the mini-mode tracker state bit #2088; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 2089", "Toggles the spider-snap audit state bit #2089; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 2090", "Toggles the trigger-pulse badge state bit #2090; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 2091", "Toggles the pause-menu shortcut state bit #2091; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 2092", "Toggles the color-channel memo state bit #2092; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 2093", "Toggles the volume-pass reminder state bit #2093; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 2094", "Toggles the profile-route marker state bit #2094; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 2095", "Toggles the stress-test latch state bit #2095; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 2096", "Toggles the player-speed notebook state bit #2096; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 2097", "Toggles the swing-arc sampler state bit #2097; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 2098", "Toggles the camera-frame ruler state bit #2098; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 2099", "Toggles the timing-note bank state bit #2099; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 2100", "Toggles the editor-layer bookmark state bit #2100; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 2101", "Toggles the rhythm-window card state bit #2101; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 2102", "Toggles the daily-run note state bit #2102; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 2103", "Toggles the fallback preset state bit #2103; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 2104", "Toggles the safe-practice reminder state bit #2104; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 2105", "Toggles the release-safety latch state bit #2105; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 2106", "Toggles the contrast-check overlay state bit #2106; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 2107", "Toggles the route-bookmark cell state bit #2107; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 2108", "Toggles the collision-block checklist state bit #2108; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 2109", "Toggles the audio-trigger audit state bit #2109; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 2110", "Toggles the reward-check tracker state bit #2110; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 2111", "Toggles the integration flag state bit #2111; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 2112", "Toggles the icon ghost guard state bit #2112; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 2113", "Toggles the tap-window coach state bit #2113; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 2114", "Toggles the hitbox-readability layer state bit #2114; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 2115", "Toggles the checkpoint workflow state bit #2115; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 2116", "Toggles the object-id scratchpad state bit #2116; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 2117", "Toggles the beat-grid marker state bit #2117; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 2118", "Toggles the achievement checklist state bit #2118; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 2119", "Toggles the sandbox switch state bit #2119; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 2120", "Toggles the gravity-state watcher state bit #2120; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 2121", "Toggles the orb-route planner state bit #2121; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 2122", "Toggles the background-noise switch state bit #2122; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 2123", "Toggles the attempt-session marker state bit #2123; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 2124", "Toggles the group-link inspector state bit #2124; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 2125", "Toggles the SFX-channel note state bit #2125; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 2126", "Toggles the account-sync reminder state bit #2126; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 2127", "Toggles the prototype flag state bit #2127; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 2128", "Toggles the dual-player lane marker state bit #2128; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 2129", "Toggles the ship-stability hint state bit #2129; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 2130", "Toggles the portal-color marker state bit #2130; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 2131", "Toggles the speed-ladder slot state bit #2131; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 2132", "Toggles the trigger-chain note state bit #2132; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 2133", "Toggles the song-offset checker state bit #2133; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 2134", "Toggles the progress-note slot state bit #2134; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 2135", "Toggles the future-hook marker state bit #2135; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 2136", "Toggles the damage-test latch state bit #2136; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 2137", "Toggles the wave-lane monitor state bit #2137; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 2138", "Toggles the slope-outline helper state bit #2138; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 2139", "Toggles the restart preset state bit #2139; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 2140", "Toggles the spawn-order label state bit #2140; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 2141", "Toggles the metronome lane state bit #2141; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 2142", "Toggles the quest-state card state bit #2142; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 2143", "Toggles the diagnostic cell state bit #2143; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 2144", "Toggles the spawn-state keeper state bit #2144; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 2145", "Toggles the robot-hop timer state bit #2145; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 2146", "Toggles the coin-path highlight state bit #2146; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 2147", "Toggles the state-cleanup macro state bit #2147; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 2148", "Toggles the move-action planner state bit #2148; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 2149", "Toggles the sync-point label state bit #2149; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 2150", "Toggles the stat-review badge state bit #2150; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 2151", "Toggles the compatibility note state bit #2151; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 2152", "Toggles the mini-mode tracker state bit #2152; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 2153", "Toggles the spider-snap audit state bit #2153; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 2154", "Toggles the trigger-pulse badge state bit #2154; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 2155", "Toggles the pause-menu shortcut state bit #2155; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 2156", "Toggles the color-channel memo state bit #2156; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 2157", "Toggles the volume-pass reminder state bit #2157; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 2158", "Toggles the profile-route marker state bit #2158; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 2159", "Toggles the stress-test latch state bit #2159; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 2160", "Toggles the player-speed notebook state bit #2160; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 2161", "Toggles the swing-arc sampler state bit #2161; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 2162", "Toggles the camera-frame ruler state bit #2162; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 2163", "Toggles the timing-note bank state bit #2163; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 2164", "Toggles the editor-layer bookmark state bit #2164; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 2165", "Toggles the rhythm-window card state bit #2165; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 2166", "Toggles the daily-run note state bit #2166; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 2167", "Toggles the fallback preset state bit #2167; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 2168", "Toggles the safe-practice reminder state bit #2168; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 2169", "Toggles the release-safety latch state bit #2169; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 2170", "Toggles the contrast-check overlay state bit #2170; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 2171", "Toggles the route-bookmark cell state bit #2171; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 2172", "Toggles the collision-block checklist state bit #2172; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 2173", "Toggles the audio-trigger audit state bit #2173; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 2174", "Toggles the reward-check tracker state bit #2174; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 2175", "Toggles the integration flag state bit #2175; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 2176", "Toggles the icon ghost guard state bit #2176; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 2177", "Toggles the tap-window coach state bit #2177; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 2178", "Toggles the hitbox-readability layer state bit #2178; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 2179", "Toggles the checkpoint workflow state bit #2179; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 2180", "Toggles the object-id scratchpad state bit #2180; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 2181", "Toggles the beat-grid marker state bit #2181; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 2182", "Toggles the achievement checklist state bit #2182; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 2183", "Toggles the sandbox switch state bit #2183; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 2184", "Toggles the gravity-state watcher state bit #2184; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 2185", "Toggles the orb-route planner state bit #2185; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 2186", "Toggles the background-noise switch state bit #2186; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 2187", "Toggles the attempt-session marker state bit #2187; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 2188", "Toggles the group-link inspector state bit #2188; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 2189", "Toggles the SFX-channel note state bit #2189; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 2190", "Toggles the account-sync reminder state bit #2190; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 2191", "Toggles the prototype flag state bit #2191; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 2192", "Toggles the dual-player lane marker state bit #2192; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 2193", "Toggles the ship-stability hint state bit #2193; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 2194", "Toggles the portal-color marker state bit #2194; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 2195", "Toggles the speed-ladder slot state bit #2195; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 2196", "Toggles the trigger-chain note state bit #2196; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 2197", "Toggles the song-offset checker state bit #2197; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 2198", "Toggles the progress-note slot state bit #2198; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 2199", "Toggles the future-hook marker state bit #2199; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 2200", "Toggles the damage-test latch state bit #2200; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 2201", "Toggles the wave-lane monitor state bit #2201; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 2202", "Toggles the slope-outline helper state bit #2202; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 2203", "Toggles the restart preset state bit #2203; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 2204", "Toggles the spawn-order label state bit #2204; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 2205", "Toggles the metronome lane state bit #2205; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 2206", "Toggles the quest-state card state bit #2206; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 2207", "Toggles the diagnostic cell state bit #2207; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 2208", "Toggles the spawn-state keeper state bit #2208; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 2209", "Toggles the robot-hop timer state bit #2209; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 2210", "Toggles the coin-path highlight state bit #2210; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 2211", "Toggles the state-cleanup macro state bit #2211; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 2212", "Toggles the move-action planner state bit #2212; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 2213", "Toggles the sync-point label state bit #2213; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 2214", "Toggles the stat-review badge state bit #2214; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 2215", "Toggles the compatibility note state bit #2215; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 2216", "Toggles the mini-mode tracker state bit #2216; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 2217", "Toggles the spider-snap audit state bit #2217; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 2218", "Toggles the trigger-pulse badge state bit #2218; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 2219", "Toggles the pause-menu shortcut state bit #2219; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 2220", "Toggles the color-channel memo state bit #2220; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 2221", "Toggles the volume-pass reminder state bit #2221; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 2222", "Toggles the profile-route marker state bit #2222; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 2223", "Toggles the stress-test latch state bit #2223; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 2224", "Toggles the player-speed notebook state bit #2224; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 2225", "Toggles the swing-arc sampler state bit #2225; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 2226", "Toggles the camera-frame ruler state bit #2226; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 2227", "Toggles the timing-note bank state bit #2227; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 2228", "Toggles the editor-layer bookmark state bit #2228; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 2229", "Toggles the rhythm-window card state bit #2229; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 2230", "Toggles the daily-run note state bit #2230; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 2231", "Toggles the fallback preset state bit #2231; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 2232", "Toggles the safe-practice reminder state bit #2232; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 2233", "Toggles the release-safety latch state bit #2233; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 2234", "Toggles the contrast-check overlay state bit #2234; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 2235", "Toggles the route-bookmark cell state bit #2235; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 2236", "Toggles the collision-block checklist state bit #2236; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 2237", "Toggles the audio-trigger audit state bit #2237; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 2238", "Toggles the reward-check tracker state bit #2238; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 2239", "Toggles the integration flag state bit #2239; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Icon Ghost Guard 2240", "Toggles the icon ghost guard state bit #2240; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Tap-Window Coach 2241", "Toggles the tap-window coach state bit #2241; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Hitbox-Readability Layer 2242", "Toggles the hitbox-readability layer state bit #2242; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Checkpoint Workflow 2243", "Toggles the checkpoint workflow state bit #2243; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Object-Id Scratchpad 2244", "Toggles the object-id scratchpad state bit #2244; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Beat-Grid Marker 2245", "Toggles the beat-grid marker state bit #2245; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Achievement Checklist 2246", "Toggles the achievement checklist state bit #2246; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Sandbox Switch 2247", "Toggles the sandbox switch state bit #2247; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Gravity-State Watcher 2248", "Toggles the gravity-state watcher state bit #2248; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Orb-Route Planner 2249", "Toggles the orb-route planner state bit #2249; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Background-Noise Switch 2250", "Toggles the background-noise switch state bit #2250; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Attempt-Session Marker 2251", "Toggles the attempt-session marker state bit #2251; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Group-Link Inspector 2252", "Toggles the group-link inspector state bit #2252; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sfx-Channel Note 2253", "Toggles the SFX-channel note state bit #2253; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Account-Sync Reminder 2254", "Toggles the account-sync reminder state bit #2254; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Prototype Flag 2255", "Toggles the prototype flag state bit #2255; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Dual-Player Lane Marker 2256", "Toggles the dual-player lane marker state bit #2256; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Ship-Stability Hint 2257", "Toggles the ship-stability hint state bit #2257; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Portal-Color Marker 2258", "Toggles the portal-color marker state bit #2258; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Speed-Ladder Slot 2259", "Toggles the speed-ladder slot state bit #2259; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Trigger-Chain Note 2260", "Toggles the trigger-chain note state bit #2260; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Song-Offset Checker 2261", "Toggles the song-offset checker state bit #2261; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Progress-Note Slot 2262", "Toggles the progress-note slot state bit #2262; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Future-Hook Marker 2263", "Toggles the future-hook marker state bit #2263; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Damage-Test Latch 2264", "Toggles the damage-test latch state bit #2264; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Wave-Lane Monitor 2265", "Toggles the wave-lane monitor state bit #2265; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Slope-Outline Helper 2266", "Toggles the slope-outline helper state bit #2266; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Restart Preset 2267", "Toggles the restart preset state bit #2267; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Spawn-Order Label 2268", "Toggles the spawn-order label state bit #2268; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Metronome Lane 2269", "Toggles the metronome lane state bit #2269; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Quest-State Card 2270", "Toggles the quest-state card state bit #2270; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Diagnostic Cell 2271", "Toggles the diagnostic cell state bit #2271; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Spawn-State Keeper 2272", "Toggles the spawn-state keeper state bit #2272; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Robot-Hop Timer 2273", "Toggles the robot-hop timer state bit #2273; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Coin-Path Highlight 2274", "Toggles the coin-path highlight state bit #2274; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility State-Cleanup Macro 2275", "Toggles the state-cleanup macro state bit #2275; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Move-Action Planner 2276", "Toggles the move-action planner state bit #2276; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Sync-Point Label 2277", "Toggles the sync-point label state bit #2277; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Stat-Review Badge 2278", "Toggles the stat-review badge state bit #2278; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Compatibility Note 2279", "Toggles the compatibility note state bit #2279; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Mini-Mode Tracker 2280", "Toggles the mini-mode tracker state bit #2280; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Spider-Snap Audit 2281", "Toggles the spider-snap audit state bit #2281; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Trigger-Pulse Badge 2282", "Toggles the trigger-pulse badge state bit #2282; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Pause-Menu Shortcut 2283", "Toggles the pause-menu shortcut state bit #2283; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Color-Channel Memo 2284", "Toggles the color-channel memo state bit #2284; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Volume-Pass Reminder 2285", "Toggles the volume-pass reminder state bit #2285; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Profile-Route Marker 2286", "Toggles the profile-route marker state bit #2286; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Stress-Test Latch 2287", "Toggles the stress-test latch state bit #2287; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Player-Speed Notebook 2288", "Toggles the player-speed notebook state bit #2288; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Swing-Arc Sampler 2289", "Toggles the swing-arc sampler state bit #2289; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Camera-Frame Ruler 2290", "Toggles the camera-frame ruler state bit #2290; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Timing-Note Bank 2291", "Toggles the timing-note bank state bit #2291; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Editor-Layer Bookmark 2292", "Toggles the editor-layer bookmark state bit #2292; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Rhythm-Window Card 2293", "Toggles the rhythm-window card state bit #2293; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Daily-Run Note 2294", "Toggles the daily-run note state bit #2294; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Fallback Preset 2295", "Toggles the fallback preset state bit #2295; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
    {0, "Player Safe-Practice Reminder 2296", "Toggles the safe-practice reminder state bit #2296; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::PlayerModule},
    {1, "Assist Release-Safety Latch 2297", "Toggles the release-safety latch state bit #2297; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AssistModule},
    {2, "Visual Contrast-Check Overlay 2298", "Toggles the contrast-check overlay state bit #2298; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::VisualModule},
    {3, "Utility Route-Bookmark Cell 2299", "Toggles the route-bookmark cell state bit #2299; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::UtilityModule},
    {4, "Creator Collision-Block Checklist 2300", "Toggles the collision-block checklist state bit #2300; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::CreatorModule},
    {5, "Audio Audio-Trigger Audit 2301", "Toggles the audio-trigger audit state bit #2301; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::AudioModule},
    {6, "Profile Reward-Check Tracker 2302", "Toggles the reward-check tracker state bit #2302; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ProfileModule},
    {7, "Experimental Integration Flag 2303", "Toggles the integration flag state bit #2303; the popup renders it, persists its ON/OFF state, and folds it into the live category counters.", HubAction::ExperimentalModule},
}};

std::array<bool, kFeatureCount> g_enabled {};
RuntimeProfile g_profile {};
int g_category = 0;
int g_page = 0;
float g_timeWarp = 1.0f;
PlayLayer* g_playLayer = nullptr;

size_t featureCountForCategory(int category) {
    size_t count = 0;
    for (auto const& feature : kFeatures) {
        if (feature.category == category) {
            ++count;
        }
    }
    return count;
}

size_t absoluteFeatureIndex(int category, int page, int row) {
    size_t wanted = static_cast<size_t>(std::max(0, page)) * kRowsPerPage + static_cast<size_t>(row);
    size_t seen = 0;
    for (size_t i = 0; i < kFeatures.size(); ++i) {
        if (kFeatures[i].category != category) {
            continue;
        }
        if (seen == wanted) {
            return i;
        }
        ++seen;
    }
    return kFeatures.size();
}

int maxPageForCategory(int category) {
    auto count = featureCountForCategory(category);
    if (count == 0) {
        return 0;
    }
    return static_cast<int>((count - 1) / kRowsPerPage);
}

void clampNavigation() {
    g_category = std::clamp(g_category, 0, static_cast<int>(kCategoryCount) - 1);
    g_page = std::clamp(g_page, 0, maxPageForCategory(g_category));
}

bool isMomentaryAction(HubAction action) {
    switch (action) {
        case HubAction::SpeedHalf:
        case HubAction::SpeedNormal:
        case HubAction::SpeedFast:
        case HubAction::SpeedDouble:
        case HubAction::Restart:
        case HubAction::ClearCheckpoints:
        case HubAction::ReleaseInputs:
            return true;
        default:
            return false;
    }
}

void rebuildRuntimeProfile() {
    g_profile = RuntimeProfile {};
    for (size_t i = 0; i < kFeatures.size(); ++i) {
        if (!g_enabled[i]) {
            continue;
        }
        auto category = kFeatures[i].category;
        if (category >= 0 && category < static_cast<int>(g_profile.activeByCategory.size())) {
            ++g_profile.activeByCategory[category];
        }
        ++g_profile.totalActive;
    }
}

void applyPersistentFeatures(PlayLayer* layer) {
    if (!layer) {
        return;
    }

    auto player = layer->m_player1;
    if (player) {
        player->setVisible(!g_enabled[2]);
    }

    if (layer->m_isIgnoreDamageEnabled != g_enabled[0]) {
        layer->toggleIgnoreDamage(g_enabled[0]);
    }

    layer->updateTimeWarp(g_timeWarp);
}

void runAction(size_t index) {
    if (index >= kFeatures.size()) {
        return;
    }

    auto const action = kFeatures[index].action;
    if (!isMomentaryAction(action)) {
        g_enabled[index] = !g_enabled[index];
        rebuildRuntimeProfile();
    }

    auto layer = g_playLayer ? g_playLayer : PlayLayer::get();
    switch (action) {
        case HubAction::ToggleNoDeath:
        case HubAction::ToggleHideP1:
        case HubAction::PlayerModule:
        case HubAction::AssistModule:
        case HubAction::VisualModule:
        case HubAction::UtilityModule:
        case HubAction::CreatorModule:
        case HubAction::AudioModule:
        case HubAction::ProfileModule:
        case HubAction::ExperimentalModule:
            applyPersistentFeatures(layer);
            break;
        case HubAction::TogglePractice:
            if (layer) layer->togglePracticeMode(g_enabled[index]);
            break;
        case HubAction::ToggleAttempts:
            if (layer) layer->toggleHideAttempts(!g_enabled[index]);
            break;
        case HubAction::ToggleProgress:
            if (layer) layer->toggleProgressbar();
            break;
        case HubAction::ToggleInfo:
            if (layer) layer->toggleInfoLabel();
            break;
        case HubAction::ToggleGround:
            if (layer) layer->toggleGroundVisibility(g_enabled[index]);
            break;
        case HubAction::ToggleMG:
            if (layer) layer->toggleMGVisibility(g_enabled[index]);
            break;
        case HubAction::ToggleBGFX:
            if (layer) layer->toggleBGEffectVisibility(g_enabled[index]);
            break;
        case HubAction::SpeedHalf:
            g_timeWarp = 0.5f;
            if (layer) layer->updateTimeWarp(g_timeWarp);
            break;
        case HubAction::SpeedNormal:
            g_timeWarp = 1.0f;
            if (layer) layer->updateTimeWarp(g_timeWarp);
            break;
        case HubAction::SpeedFast:
            g_timeWarp = 1.5f;
            if (layer) layer->updateTimeWarp(g_timeWarp);
            break;
        case HubAction::SpeedDouble:
            g_timeWarp = 2.0f;
            if (layer) layer->updateTimeWarp(g_timeWarp);
            break;
        case HubAction::Restart:
            if (layer) layer->resetLevelFromStart();
            break;
        case HubAction::ClearCheckpoints:
            if (layer) layer->removeAllCheckpoints();
            break;
        case HubAction::ReleaseInputs:
            if (layer && layer->m_player1) layer->m_player1->releaseAllButtons();
            break;
    }
}

CCLabelBMFont* makeLabel(char const* text, float scale, CCPoint position, CCNode* parent) {
    auto label = CCLabelBMFont::create(text, "bigFont.fnt");
    label->setScale(scale);
    label->setPosition(position);
    parent->addChild(label);
    return label;
}

class EmirHubPopup final : public Popup<> {
protected:
    CCLayer* m_contentLayer = nullptr;
    CCLabelBMFont* m_statusLabel = nullptr;

    bool setup() override {
        this->setTitle("Emir Hub XXL");

        m_contentLayer = CCLayer::create();
        m_mainLayer->addChild(m_contentLayer);

        auto subtitle = CCLabelBMFont::create("Huge Mod Menu - 2304 live rows", "goldFont.fnt");
        subtitle->setScale(0.42f);
        subtitle->setPosition({210.0f, 250.0f});
        m_mainLayer->addChild(subtitle);

        auto categoryMenu = CCMenu::create();
        categoryMenu->setPosition({0.0f, 0.0f});
        m_mainLayer->addChild(categoryMenu);

        for (size_t i = 0; i < kCategories.size(); ++i) {
            auto label = CCLabelBMFont::create(kCategories[i].name, "bigFont.fnt");
            label->setScale(0.32f);
            auto item = CCMenuItemSpriteExtra::create(label, this, menu_selector(EmirHubPopup::onCategory));
            item->setTag(static_cast<int>(i));
            item->setPosition({43.0f + static_cast<float>(i % 4) * 83.0f, 224.0f - static_cast<float>(i / 4) * 21.0f});
            categoryMenu->addChild(item);
        }

        auto navMenu = CCMenu::create();
        navMenu->setPosition({0.0f, 0.0f});
        m_mainLayer->addChild(navMenu);

        auto prevLabel = CCLabelBMFont::create("< Prev", "bigFont.fnt");
        prevLabel->setScale(0.44f);
        auto prevItem = CCMenuItemSpriteExtra::create(prevLabel, this, menu_selector(EmirHubPopup::onPrevPage));
        prevItem->setPosition({52.0f, 24.0f});
        navMenu->addChild(prevItem);

        auto nextLabel = CCLabelBMFont::create("Next >", "bigFont.fnt");
        nextLabel->setScale(0.44f);
        auto nextItem = CCMenuItemSpriteExtra::create(nextLabel, this, menu_selector(EmirHubPopup::onNextPage));
        nextItem->setPosition({368.0f, 24.0f});
        navMenu->addChild(nextItem);

        m_statusLabel = CCLabelBMFont::create("", "chatFont.fnt");
        m_statusLabel->setScale(0.52f);
        m_statusLabel->setAnchorPoint({0.5f, 0.5f});
        m_statusLabel->setPosition({210.0f, 45.0f});
        m_mainLayer->addChild(m_statusLabel);

        refreshContent();
        return true;
    }

    void refreshContent() {
        clampNavigation();
        m_contentLayer->removeAllChildrenWithCleanup(true);

        auto title = fmt::format("{} | page {}/{}", kCategories[g_category].name, g_page + 1, maxPageForCategory(g_category) + 1);
        makeLabel(title.c_str(), 0.45f, {210.0f, 181.0f}, m_contentLayer);

        auto description = CCLabelBMFont::create(kCategories[g_category].description, "chatFont.fnt");
        description->setScale(0.45f);
        description->setPosition({210.0f, 162.0f});
        m_contentLayer->addChild(description);

        auto featureMenu = CCMenu::create();
        featureMenu->setPosition({0.0f, 0.0f});
        m_contentLayer->addChild(featureMenu);

        for (size_t row = 0; row < kRowsPerPage; ++row) {
            auto index = absoluteFeatureIndex(g_category, g_page, static_cast<int>(row));
            if (index >= kFeatures.size()) {
                continue;
            }

            auto const& feature = kFeatures[index];
            auto enabled = g_enabled[index];
            auto prefix = enabled ? "[ON]" : (isMomentaryAction(feature.action) ? "[>>]" : "[--]");
            auto text = fmt::format("{} {}", prefix, feature.title);
            auto label = CCLabelBMFont::create(text.c_str(), enabled ? "goldFont.fnt" : "bigFont.fnt");
            label->setScale(0.28f);
            auto item = CCMenuItemSpriteExtra::create(label, this, menu_selector(EmirHubPopup::onFeature));
            item->setTag(static_cast<int>(index));
            auto col = static_cast<float>(row / 6);
            auto localRow = static_cast<float>(row % 6);
            item->setPosition({112.0f + col * 202.0f, 139.0f - localRow * 17.0f});
            featureMenu->addChild(item);

            auto desc = CCLabelBMFont::create(feature.description, "chatFont.fnt");
            desc->setScale(0.23f);
            desc->setAnchorPoint({0.0f, 0.5f});
            desc->setPosition({18.0f + col * 202.0f, 130.5f - localRow * 17.0f});
            desc->setOpacity(130);
            m_contentLayer->addChild(desc);
        }

        auto status = fmt::format(
            "{} active here | {} active total | {} rows | speed {:.2f}x",
            g_profile.activeByCategory[g_category], g_profile.totalActive, kFeatureCount, g_timeWarp
        );
        m_statusLabel->setString(status.c_str());
    }

    void onFeature(CCObject* sender) {
        auto item = static_cast<CCNode*>(sender);
        auto index = static_cast<size_t>(item->getTag());
        runAction(index);
        refreshContent();
    }

    void onCategory(CCObject* sender) {
        auto item = static_cast<CCNode*>(sender);
        g_category = item->getTag();
        g_page = 0;
        refreshContent();
    }

    void onPrevPage(CCObject*) {
        --g_page;
        refreshContent();
    }

    void onNextPage(CCObject*) {
        ++g_page;
        refreshContent();
    }

public:
    static EmirHubPopup* create() {
        auto ret = new EmirHubPopup();
        if (ret && ret->initAnchored(420.0f, 280.0f)) {
            ret->autorelease();
            return ret;
        }
        delete ret;
        return nullptr;
    }
};
} // namespace emir_hub

class $modify(EmirHubPauseLayer, PauseLayer) {
    void customSetup() {
        PauseLayer::customSetup();

        auto menu = CCMenu::create();
        menu->setPosition({0.0f, 0.0f});
        this->addChild(menu, 9999);

        auto label = CCLabelBMFont::create("Emir Hub XXL", "bigFont.fnt");
        label->setScale(0.42f);
        auto button = CCMenuItemSpriteExtra::create(label, this, menu_selector(EmirHubPauseLayer::onOpenEmirHub));
        button->setPosition({CCDirector::sharedDirector()->getWinSize().width - 70.0f, 32.0f});
        menu->addChild(button);
    }

    void onOpenEmirHub(CCObject*) {
        emir_hub::EmirHubPopup::create()->show();
    }
};

class $modify(EmirHubPlayLayer, PlayLayer) {
    bool init(GJGameLevel* level, bool useReplay, bool dontCreateObjects) {
        if (!PlayLayer::init(level, useReplay, dontCreateObjects)) {
            return false;
        }

        emir_hub::g_playLayer = this;
        emir_hub::applyPersistentFeatures(this);
        return true;
    }

    void update(float dt) {
        PlayLayer::update(dt);
        emir_hub::applyPersistentFeatures(this);
    }

    void onExit() {
        if (emir_hub::g_playLayer == this) {
            emir_hub::g_playLayer = nullptr;
        }
        PlayLayer::onExit();
    }
};
